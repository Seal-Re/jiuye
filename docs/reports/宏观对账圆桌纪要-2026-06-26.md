# 宏观对账圆桌纪要 — 2026-06-26

> **性质**：多-agent 圆桌审计纪要（用户授权 · 落盘存档）。3 审计席（Opus，红线 B.7）+ 主控独立核验（A.3）。
> **基准**：git HEAD `5e0d019`（drama-013 收官）｜ 分支 `feat/cultivation-a2`｜ `dotnet test` = **1051 绿 / 0 失败 / 0 跳过**（主控实测）。
> **议题**：① 文件排布合规 ② 文档时效性 ③ 宏观基线是否走偏。
> **方法**：三席只读审计并行 → 主控逐条复核关键断言（不认子代理自报）→ 合议裁决 → 本纪要。

---

## 0. 主控核验声明（红线 A.3）

本纪要中标【主控已核验】的断言，均由主控独立跑命令 / 读源码确认，**非转述子代理自报**。三席发现里两条最关键的论点**互相冲突**，主控以代码为准裁决（见 §1）。其余低风险条目（如本地 `pixel/` 体积、`Chronicle.cs` 叙事投影质感）按席位证据采信、标注来源，未逐一复跑。

---

## 1. 🔴 keystone 裁决：`balance-cross` 是**假 Done**（红线 A.3 + A.4 违规）

**这是本次圆桌最重要的发现，且主控在此推翻了 Seat B、采信 Seat C。**

Seat C 指控 `balance-cross/EPIC.md` 写 Done 但 DoD 全空（假 Done）；Seat B 的订正清单却把 balance-cross 当 Done、要求勾上 INV-CROSS 验收。**两席直接矛盾**。主控读代码+台账+git 裁决如下：

### 四个真相源互相打架【主控已核验，逐一读源】

| 真相源 | 对 balance-cross / balance-003 的表述 | 文件证据 |
|---|---|---|
| `balance-cross/EPIC.md` header | **Done** | 第 4 行 `**Status**: Done` |
| 同文件 DoD | **5/5 未勾** + Summary "🔴 最大功能缺口，同 UT 24-70× 失衡" | 第 11、25–29 行 |
| `index.md` #3 | **Designed** | 第 16 行 |
| `sprint-status.yaml` `balance-003` | **done**（"A.6 订正 git证实"） | 第 ~50 行 |
| `story-003-c1-convergence-40-60.md` | **Deferred** | header `Status: Deferred`，"Carried from sprint 3, deferred again in sprint 4" |

### 代码才是地面真相，它说 NOT Done【主控已核验】

- 实际测试方法名 = `C1Gate_CombatPaths_SameUT_WinRate_Within_**35_65**`（**不是**契约承诺的 [40,60]%）。
- 该 gate 是 **advisory**：`const int KnownViolationBaseline = 47; KnownTestedBaseline = 48;` —— 断言 `violations <= 47`，即**显式把 48 对中的 47 对同-UT 失衡钉为"已知违规基线"**放行。
- **全仓不存在任何 `Within_40_60` 测试**（`grep -rln "Within_40_60" tests/` → 空）。契约 C1（整个 epic 的存在理由）**未兑现**。
- git：`76a9a17 chore: defer balance-003 — revert quick multiplier tweaks…` —— [40,60]% 硬闸门 + 模块伤害钳制 + RealmMultipliers 重校准 **被 defer**，从未完成。

### 裁决
**`balance-cross` 真实状态 = Designed/Partially（balance-001/002 派生视图+advisory gate 已做；balance-003 收敛硬闸门 Deferred）。`EPIC.md` 的 `Status: Done` 是误标。** 在一个**第一红线就是"防虚报"**的项目里，把自承的"最大功能缺口"标成 Done，是比任何技术债都该先堵的信任裂缝。

> 公平注记：此项目审计纪律本身很强（前审计主动揪出 combat-r2/cultivation-a2 陈旧标记并订正，复审主控推翻 4 条子代理误报）。这个 balance-cross 矛盾更像**一次漏网的标记错误**，非系统性造假——但正因纪律强，更该补。

**整改（零成本，最高优先）**：`balance-cross/EPIC.md` 第 4 行 `Done` → `Designed`；与 index.md 对齐；DoD 5 项诚实保留 `[ ]`。`sprint-status.yaml` 的 `balance-003 done` 同步订正为 deferred（git `76a9a17` 证实 defer）。

---

## 2. Seat A — 文件排布 / 仓库卫生：**尚可**

总体：核心工程目录（`src`/`tests`/`tools/pixel-pipeline`/`production`）清晰合规，**零构建产物误提交**，`.gitignore` 对 bin/obj/`__pycache__`/session-state 覆盖正确。问题集中在低风险卫生债。

| 严重度 | 发现 | 证据（主控复核状态） | 整改 |
|---|---|---|---|
| 中 | `origin/HEAD` 指向陈旧分支 | 【主控已核验】`origin/HEAD → feat/jianghu-v1.2-B5-modules`，该分支 **146 落后 / 0 领先** master | `git remote set-head origin master`（需用户拍板：影响 clone 默认分支） |
| 中 | 3 条远端分支堆积 | 【主控已核验】B5-modules(146/0)、B5-batch4-duelengine(113/0)、claude-md-refresh(**94/1**，领先 1 个 docs commit `370c127`) | 删 B5-modules/B5-batch4（纯陈旧）；claude-md-refresh 删前先确认 `370c127` 已吸收。**删远端需用户确认**（红线） |
| 中 | 错位 tracked 文件 `output/map_seed42_scale2.png` | 【主控已核验】`git ls-files output/` 命中该 png；`.gitignore` 仅 `*.output`（**不匹配 `output/` 目录**） | 推荐 `git rm` + `.gitignore` 补 `output/`（可重生成的验证产物）；或移入 `assets/art/pixel/`。涉删 tracked，需批准 |
| 低 | `TASKS.md` 派生指针失效路径 + 陈旧快照 | 指向不存在的 `docs/adoption-plan-2026-06-15.md`（真实在 `docs/reports/采用迁移计划.md`）；快照停在 410绿/Sprint2 | 修路径；快照块改为"跑 `/sprint-status`"一行（杜绝再腐烂）。无风险 |
| 低 | 本地 `pixel/` 残留（Seat A 报 8.1M，0 tracked，已迁走）+ `.gitignore` dead rule | `git ls-files pixel/` → 0 tracked；`.gitignore` 第 38–39 行 `pixel/_aigen/` `pixel/out/` 是给已迁目录留的死规则 | `rm -rf pixel/` + 删两条 dead rule。无风险（属 `repo-tidy` EPIC 范畴） |

**Seat A 判为健康、排除嫌疑**：像素三处职责正交（`tools/pixel-pipeline`=源 / `tests/pixel-pipeline`=测试 / `assets/art/pixel`=资产，非重复）；`production/session-state`+`session-logs` 正确 gitignored；`docs/superpowers/`（7 tracked spec/plan）是真实在用的设计源，仅**未登记进 CLAUDE.md §C 文档地图**（建议补一行）。

---

## 3. Seat B — 文档时效性：**明显漂移**（局部接近严重失真）

红线本轮已订正的 CLAUDE.md / index.md / sprint-status / 5 个 EPIC header 已与代码对齐。但**下游文档大面积滞后**，会主动误导未来 agent 把已建系统当未建。

### 高优先（会误导现状判断）【主控已逐条核验】

| 文件 | 现状（陈旧） | 应为 | 风险 |
|---|---|---|---|
| `design/gdd/systems-index.md` 第 17–32 行 | combat=In Progress；drama-engine/map-system/faction/cultivation-a2/a3/balance-cross=**Designed**；integration=Not Designed | 对齐 index.md：drama=Done、map=Wired、faction=C.0 Done、cultivation-a2=Done、combat=Done、integration=Partially wired（balance-cross 见 §1=Designed，此项 systems-index **恰好对、index.md/EPIC 才错**） | **最高杠杆**：系统全景图，新 agent 会重复设计已收官 epic |
| `production/epics/map-system/EPIC.md` 第 4、26 行 | `Status: Designed (0 code)`、`设计完零代码` | `Wired`（`--map` 接线实证：Program.cs / WorldFactory / World.Clone） | production/ **内部硬矛盾**（与 index.md #8=Wired 打架，A.2 违规） |
| `design/gdd/drama-system.md` 第 8、45 行 | "仅薄工具层——核心引擎尚未建"、"核心引擎待建" | 删/改：账本/复仇弧/跨代继承均已 Done（同文件 §9 表全 ✅） | **文档内部自相矛盾**（头部"待建" vs §9"✅"） |
| `design/gdd/cultivation-system.md` AC + §3.4/3.5 | A.1 余项 / A.2 两条 AC `[ ]`；§3.4/3.5 标"（设计未建）" | 勾 `[x]`（cultivation-a1-rest / a2 均 Done）；去"未建" | 把已验收系统当未建，重复实现 |
| `design/gdd/game-concept.md` 第 120 行 | INV-CROSS AC `[ ]` | **保持 `[ ]`**（见 §1：balance-cross 实为未完成——此处 Seat B 原建议勾 `[x]` 被主控否决） | （主控修正 Seat B）勾错会强化假 Done |
| `docs/agent-guide/修炼系统架构.md` 第 153 行 | "当前 350 绿" + 全文 0 处 Drama/Map/Faction | "1051 绿" + 补三命名空间 Pump 接线段 | "新 agent 必读"，350 是约 3× 前数字 |
| `docs/agent-guide/开发方式.md` 第 21、31 行 | "当前基线 350 绿，不退" | "1051 绿" | TDD"不退"基线锚点失效 |

> ⚠️ **主控修正 Seat B 一处错误**：Seat B 把 `game-concept.md` INV-CROSS AC 和 balance-cross 当作"已 Done 应勾"。§1 已证伪——INV-CROSS [40,60]% 契约未兑现，该 AC **必须保持 `[ ]`**。

### 合法历史快照（**豁免，勿删改**）【Seat B 列举，主控认可】
- `index.md` 第 10–13 行 38/204/232 测试（标"历史"）；`代码复审报告-2026-06-25.md`（带 HEAD `3cd4972`/850 绿快照）；`战斗模块分类报表.md`（派生视图，标时点）；`docs/legacy-specs/` 全部（CLAUDE.md §C 明示原地保留）；`combat-system.md` 第 303 行"即时窗口/夺舍待建"（确实未建）；`cultivation-system.md` §3.6 A.3（cultivation-a3=Designed，确实未建）。
- **Seat B 不确定项已由主控核实**：`tr-registry.yaml` / `control-manifest.md` **均不存在**（`git ls-files` 仅命中 `create-control-manifest` 的 SKILL.md）→ `采用迁移计划.md` 里这两项的 `[ ]` **是对的，非陈旧**。

---

## 4. Seat C — 宏观基线：**轻度偏**（地基扎实，重心漂移 + 一处台账失真）

**核心结论**：九野没走进死胡同——它把**确定性涌现引擎的地基打得异常扎实**（这是对的）；但在"让角色有脑"（`llm-brain`，自标的原始核心愿景）零进展时，持续往修炼/战斗/戏剧 substrate 加深度，且**最该做的 balance-cross 被假 Done 掩盖**（§1）。这不是工程质量问题，是**价值排序问题**。

### 愿景-现实对照【epic 状态主控已核验】

| 愿景要素 | 状态 | 差距 |
|---|---|---|
| 涌现模拟内核（确定性/事件驱动/生老死） | ✅ 扎实（v1.0 38测试 + off 逐字节铁律 + Pcg32 Split 流隔离） | 无差距，项目护城河 |
| 修炼战斗 substrate（21 路 / 模块化战斗） | ✅ **大幅超建**（cultivation A.0/A.1/A.2 + combat-r2 Done，占 1051 测试大头） | 超出 MVP 所需深度——over-build 主嫌 |
| 涌现戏剧性（恩怨/复仇/跨代） | ✅ 已接线，**部分兑现 Fantasy**（`Chronicle.cs` 真投影"立誓复仇/手刃仇人/父债子偿"可读叙事；Seat C 引，主控注：drama-engine Done 已核验，叙事文本质感未逐字复核） | 正向惊喜；但是 storylet 脚本化涌现，非智能涌现 |
| **LLM 脑**（黑盒 API 多智能体） | 🔴 **零进展**（`llm-brain/EPIC.md` 空壳，"未设计未建"） | **最大愿景差距** |
| 可视化（编年史可看） | 🟡 仅 spike（Python 渲染器跑假拓扑，与 C# 世界无序列化桥） | 大差距，外人看不到 |
| Unity 玩家介入 | ⬜ 未动（预期内远期，**不算偏**） | — |
| 平衡可信度（同 UT 40–60%） | 🔴 **未实现 + 台账矛盾**（§1） | 致命：21 路在战斗里数值名存实亡（24–70× 碾压） |

### Seat C 五问精要
1. **健康打地基 or 过度建造空齿轮箱？** → 介于两者，偏"地基对、但已超厚，齿轮箱缺两个关键齿轮（脑 + 平衡）"。drama 证明齿轮在转、能出戏，非空转；但优先级被**工程美感（系统完整性）**主导，而非**产品价值**。
2. **核心循环现在可玩吗？** → 已是"可看的最小核心"（`dotnet run` 产入道/破境/切磋/门派/复仇录），但"活得机械"：(a) 数值失真（平衡缺），(b) 模板化（脑缺）。**不完全等 llm-brain 才活，但讲的故事不够有趣。**
3. **下一个最该做（产品价值排序）**：① balance-cross（让已沉没的 combat 投入兑现价值，小投入大回报）② 可视化最小桥（C#→JSON→渲染，产品门面）③ llm-brain **先出 GDD 定护栏**（高价值高风险，别裸冲实现）④ integration story-009 收口。
4. **Scope creep**：cultivation A.2 26 story / A.3 排队 = premature 精雕主嫌；drama 跨代继承"做对了但做早了"。建议**冻结 cultivation-a3、combat-fullstruct**，**降优先 a1-rest/a2 余项、faction C.1**。21 路全建属红线 A.2 有意承诺，不算 creep（但广度建了、平衡深度欠了）。
5. **里程碑健康度**：`stage.txt = Production` **不合理**，真实是 **Late Pre-Production / Vertical-Slice**——Production 前提是"核心循环已验证有趣并锁定"，但平衡未验证、卖点未设计。

### Seat C 一句话制作人建议
> 你已把地基和齿轮箱打磨得超出 MVP 所需了——请把力气从"沿依赖图继续往上盖"转向"**修好平衡（让 21 路战斗可信）+ 做个最小可视化（让编年史能给外人看）**"；并立刻订正那个写着 Done 却 DoD 全空的 balance-cross 标记。

---

## 5. 合议裁决 · 整改优先级（主控综合）

三席 + 主控核验收敛到一条主线：**项目工程地基与纪律是强的，但"台账真相"和"价值排序"出现漂移，需一次校准。** 按"零成本守红线 → 灭误导 → 战略再校准"分层：

### P0 — 立即（零成本，守红线 A.2/A.3/A.4，纯台账订正）
1. **订正 balance-cross 假 Done**：`EPIC.md` Done→Designed；`sprint-status.yaml` balance-003 done→deferred；index.md 已是 Designed（对）。〔§1〕
2. **修 production/ 内部硬矛盾**：`map-system/EPIC.md` `Designed (0 code)`→`Wired`。〔§3〕
3. **修 stage.txt**：`Production`→`Pre-Production / Vertical-Slice`（或加注说明）。〔§4〕

### P1 — 高优先（灭"误导未来 agent"的活跃陷阱，纯文档 Edit，零代码风险）
4. `systems-index.md` Status 列全表对齐 index.md（6+ 系统）。
5. `drama-system.md` 删头部"核心引擎尚未建"（消内部矛盾）。
6. `cultivation-system.md` 勾 A.1 余项/A.2 AC、去 §3.4/3.5"未建"。
7. `agent-guide/{修炼系统架构,开发方式}.md` 350→1051 + 补 Drama/Map/Faction。
8. **保持** `game-concept.md` INV-CROSS AC `[ ]`（主控否决 Seat B 的勾 `[x]`）。

### P2 — 仓库卫生（部分需用户拍板）
9. 无风险即办：`rm -rf pixel/` + 删 `.gitignore` dead rule；修 `TASKS.md` 失效路径+陈旧快照；CLAUDE.md §C 补 `docs/superpowers/`。
10. 需用户确认：`output/*.png` 决断（rm + ignore）；`git remote set-head origin master`；删陈旧远端分支（B5-modules / B5-batch4-duelengine；claude-md-refresh 先核 `370c127`）。
> 多数已在 `repo-tidy` EPIC（Todo）登记，可借此推进时清账。

### P3 — 战略再校准（**大方向，交用户决策，红线 A.1**）
11. **价值排序**（Seat C 建议，待用户拍板）：balance-cross 实现 → 可视化最小桥 → llm-brain 出 GDD → integration story-009 收口。
12. **冻结/降优先**：冻结 cultivation-a3 / combat-fullstruct；降 a1-rest、a2 余项、faction C.1。

---

## 6. 本纪要的边界（诚实声明）
- 本纪要**只读审计 + 落盘**，**未改动任何被审计文件**（除前序已单独批准的 CLAUDE.md 订正）。P0–P2 整改、P3 再校准均**待用户决策后执行**（红线 A.1：大方向交用户）。
- 主控核验聚焦**高风险/相互冲突**断言（§1 balance-cross、map-system、output/、分支、systems-index、agent-guide、tr-registry 存在性）。低风险描述性内容（`pixel/` 体积、`Chronicle.cs` 叙事质感）按席位证据采信并标注来源。
- 三席原始完整发现（含证据命令）已并入上文；如需逐席原文可回溯本会话。

---

*圆桌出席：Seat A（仓库卫生·Opus）、Seat B（文档时效·Opus）、Seat C（宏观基线·Opus/game-designer）、Chair（主控核验综合·Opus）。HEAD `5e0d019`，1051 绿。*
