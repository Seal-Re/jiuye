# 江湖涌现模拟内核 v1.0 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 实现 v1.0 walking skeleton——一个确定性、开放式、事件驱动的多智能体武侠江湖模拟内核（纯 C# 核 + CLI），用 RuleBrain 跑通"生成人设→自主行动→生老死+新人涌现→编年史"。

**Architecture:** `Jianghu.Core`（`netstandard2.1` 纯逻辑库，端口反转、确定性）+ `Jianghu.Cli`（`net8.0` 控制台宿主）+ `Jianghu.Core.Tests`（`net8.0` xUnit）。所有随机走注入的整数 PRNG；事件驱动调度器替代固定回合；DomainEvent 单源投影到 Memory/Relations/Chronicle；单步预算防卡死。

**Tech Stack:** .NET SDK 8、C# latest、xUnit、Microsoft.CodeAnalysis.BannedApiAnalyzers。无 LLM、无 JSON（模板=代码常量；快照=深拷贝）——LLM/序列化是 v1.1。

**追溯：** 任务回引设计规格 `docs/superpowers/specs/2026-06-13-武侠人设生成-江湖涌现模拟内核-design.md` 的需求 ID（§17）：R-F1..F7 / R-NF1..F7。

---

## 文件结构（决定分解边界）

```
Jianghu.sln
src/Jianghu.Core/
  Jianghu.Core.csproj            # netstandard2.1, BannedApiAnalyzers
  Compat/IsExternalInit.cs       # netstandard2.1 上 record/init 必需的 shim
  Random/IRandom.cs              # 端口：整数 PRNG 契约（GetState/SetState/Split）
  Random/Pcg32.cs                # 确定性 PCG32 实现
  Random/RandomExtensions.cs     # NextInclusive、Fisher-Yates Shuffle
  Config/LimitsConfig.cs         # 所有上限 + 可行域断言（Validate）
  Stats/StatKind.cs              # 武力/内力/根骨/悟性 枚举
  Stats/StatBlock.cs             # 属性容器 + 单一钳制 chokepoint Apply
  Stats/StatGenerator.cs         # 偏中庸、定和+封顶、无拒绝采样
  Model/CharacterId.cs           # 不可变主键
  Model/Persona.cs               # 背景 + Archetype 枚举
  Model/Goal.cs                  # 目标（带进度）
  Model/MemoryEntry.cs           # 结构化记忆条目
  Model/MemoryStore.cs           # 聚合根私有记忆（Remember/Recall）
  Model/Relations.cs             # 有向好感图
  Model/NodeId.cs / WorldNode.cs # 抽象地点
  Model/Sect.cs                  # 门派（v1.0 单个硬编码）
  Model/Character.cs             # 聚合根
  Events/DomainEvent.cs          # 不可变事件层级（唯一真相源）
  Events/Chronicle.cs            # DomainEvent → 文本投影
  Decide/DecisionContext.cs      # 角色可感知子集（不含 World）
  Decide/IBrain.cs               # 异步决策端口
  Decide/RuleBrain.cs            # 目标驱动效用机
  Actions/ActionType.cs / ActionChoice.cs
  Actions/IAction.cs / ValidationResult.cs
  Actions/TrainAction.cs / TravelAction.cs / SparAction.cs
  Actions/ActionSystem.cs        # 按 ActionType 分发 + 经 StatBlock.Apply 改世界
  Sim/Scheduler.cs               # (NextActAt, CharacterId) 最小堆
  Sim/Lifecycle.cs               # 衰老/死亡 + MaybeSpawn renewal
  Sim/World.cs                   # 世界状态 + Advance(budget) + Clone 快照
  Sim/WorldFactory.cs            # 初始世界构建 + 人设生成器
src/Jianghu.Cli/
  Jianghu.Cli.csproj             # net8.0
  Program.cs                     # 配置/种子/驱动 Advance/打印编年史+人设卡
tests/Jianghu.Core.Tests/
  Jianghu.Core.Tests.csproj      # net8.0 + xunit + 引用 Core
  （每任务对应一个测试文件，见各任务）
```

每个文件单一职责；Brain 只吃 `DecisionContext` 不吃 `World`（R-NF1 可测）；属性只经 `StatBlock.Apply`（单一 chokepoint）。

---

### Task 0: 解决方案脚手架 + Core 纯净门控

**Files:**
- Create: `Jianghu.sln`, `src/Jianghu.Core/Jianghu.Core.csproj`, `src/Jianghu.Core/Compat/IsExternalInit.cs`, `src/Jianghu.Core/BannedSymbols.txt`, `.editorconfig`, `src/Jianghu.Cli/Jianghu.Cli.csproj`, `src/Jianghu.Cli/Program.cs`, `tests/Jianghu.Core.Tests/Jianghu.Core.Tests.csproj`, `tests/Jianghu.Core.Tests/SmokeTest.cs`

- [ ] **Step 1: 建解决方案与工程**

Run:
```bash
cd "D:/AgentWorkStation/Any/武侠人设生成"
dotnet new sln -n Jianghu
dotnet new classlib -n Jianghu.Core -o src/Jianghu.Core -f netstandard2.1
dotnet new console  -n Jianghu.Cli  -o src/Jianghu.Cli  -f net8.0
dotnet new xunit    -n Jianghu.Core.Tests -o tests/Jianghu.Core.Tests -f net8.0
rm src/Jianghu.Core/Class1.cs
dotnet sln add src/Jianghu.Core src/Jianghu.Cli tests/Jianghu.Core.Tests
dotnet add src/Jianghu.Cli reference src/Jianghu.Core
dotnet add tests/Jianghu.Core.Tests reference src/Jianghu.Core
dotnet add src/Jianghu.Core package Microsoft.CodeAnalysis.BannedApiAnalyzers
```

- [ ] **Step 2: Core csproj —— 锁 netstandard2.1 + LangVersion + 禁用清单 (R-NF4)**

Replace `src/Jianghu.Core/Jianghu.Core.csproj` with:
```xml
<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <TargetFramework>netstandard2.1</TargetFramework>
    <LangVersion>latest</LangVersion>
    <Nullable>enable</Nullable>
    <ImplicitUsings>disable</ImplicitUsings>
    <TreatWarningsAsErrors>true</TreatWarningsAsErrors>
  </PropertyGroup>
  <ItemGroup>
    <AdditionalFiles Include="BannedSymbols.txt" />
  </ItemGroup>
</Project>
```

- [ ] **Step 3: 禁用清单 + IsExternalInit shim**

Create `src/Jianghu.Core/BannedSymbols.txt`:
```
T:System.Random;改用注入的 IRandom（System.Random 跨运行时不确定）
T:System.Console;Core 不做 IO
T:System.DateTime;确定性禁用墙钟（改逻辑时钟）
M:System.DateTime.get_Now;确定性禁用墙钟
T:System.Threading.Thread;Core 不感知线程
```
Create `.editorconfig` (项目根):
```ini
root = true
[*.cs]
dotnet_diagnostic.RS0030.severity = error
```
Create `src/Jianghu.Core/Compat/IsExternalInit.cs` (netstandard2.1 上 record/init 必需):
```csharp
namespace System.Runtime.CompilerServices
{
    internal static class IsExternalInit { }
}
```

- [ ] **Step 4: 冒烟测试**

Create `tests/Jianghu.Core.Tests/SmokeTest.cs`:
```csharp
using Xunit;

public class SmokeTest
{
    [Fact]
    public void Core_assembly_loads()
    {
        Assert.NotNull(typeof(Jianghu.Random.Pcg32).Assembly);
    }
}
```
This will not compile yet (Pcg32 absent) — that's the next task's anchor. For Task 0, temporarily assert on the test assembly:
```csharp
using Xunit;

public class SmokeTest
{
    [Fact]
    public void Test_project_builds() => Assert.True(true);
}
```

- [ ] **Step 5: 构建 + 测试通过**

Run:
```bash
dotnet build
dotnet test
```
Expected: build SUCCESS（Core 在裸 netstandard2.1 下编译，无禁用 API），test PASS (1 passed)。

- [ ] **Step 6: Commit**

```bash
git add Jianghu.sln src tests .editorconfig
git commit -m "chore: 脚手架 Core(netstandard2.1)+Cli+Tests, 纯净门控(BannedApi)"
```

---

### Task 1: 确定性 PRNG（Pcg32）+ 固定向量 (R-NF1)

**Files:**
- Create: `src/Jianghu.Core/Random/IRandom.cs`, `src/Jianghu.Core/Random/Pcg32.cs`
- Test: `tests/Jianghu.Core.Tests/Pcg32Tests.cs`

- [ ] **Step 1: 写失败测试（固定向量 + 状态往返 + Split 独立）**

Create `tests/Jianghu.Core.Tests/Pcg32Tests.cs`:
```csharp
using Jianghu.Random;
using Xunit;

public class Pcg32Tests
{
    [Fact]
    public void Fixed_vector_is_stable_across_runs()
    {
        var r = new Pcg32(seed: 42, stream: 54);
        uint[] got = { r.NextUInt(), r.NextUInt(), r.NextUInt() };
        // 黄金向量：实现完成后用首次输出回填，固化跨运行/跨平台一致
        uint[] golden = { 0xA15C02B7u, 0x7B47F409u, 0xBA1D3330u };
        Assert.Equal(golden, got);
    }

    [Fact]
    public void GetState_SetState_restores_full_stream()
    {
        var r = new Pcg32(1, 7);
        for (int i = 0; i < 5; i++) r.NextUInt();
        ulong[] snap = r.GetState();
        uint[] expect = new uint[5];
        for (int i = 0; i < 5; i++) expect[i] = r.NextUInt();

        var r2 = new Pcg32(999, 999);          // 不同 inc，确保恢复的是完整状态
        r2.SetState(snap);
        for (int i = 0; i < 5; i++) Assert.Equal(expect[i], r2.NextUInt()); // 整段逐值一致
    }

    [Fact]
    public void Split_streams_are_independent_and_deterministic()
    {
        var a1 = new Pcg32(7, 1).Split(100);
        var a2 = new Pcg32(7, 1).Split(100);
        var b  = new Pcg32(7, 1).Split(200);
        Assert.Equal(a1.NextUInt(), a2.NextUInt());      // 同源同 streamId → 一致
        Assert.NotEqual(a1.NextUInt(), b.NextUInt());     // 不同 streamId → 分流
    }
}
```

- [ ] **Step 2: 运行验证失败**

Run: `dotnet test --filter Pcg32Tests`
Expected: 编译失败（`Jianghu.Random.Pcg32` 不存在）。

- [ ] **Step 3: 实现 IRandom + Pcg32**

Create `src/Jianghu.Core/Random/IRandom.cs`:
```csharp
namespace Jianghu.Random
{
    /// <summary>整数确定性 PRNG 端口。所有随机必须经此。</summary>
    public interface IRandom
    {
        uint NextUInt();
        int NextInt(int maxExclusive);             // [0, maxExclusive)
        int NextInclusive(int minInclusive, int maxInclusive);
        ulong[] GetState();
        void SetState(ulong[] state);
        IRandom Split(ulong streamId);             // 派生独立子流（spawn/per-character）
    }
}
```
Create `src/Jianghu.Core/Random/Pcg32.cs`:
```csharp
using System;

namespace Jianghu.Random
{
    /// <summary>PCG-XSH-RR 32（O'Neill）。纯整数，确定性，可序列化状态。</summary>
    public sealed class Pcg32 : IRandom
    {
        private ulong _state;
        private ulong _inc;   // 非 readonly：SetState 须恢复完整状态(含 _inc)以支撑 R-NF2 续跑复现
        private const ulong Mult = 6364136223846793005UL;

        public Pcg32(ulong seed, ulong stream)
        {
            _inc = (stream << 1) | 1UL;
            _state = 0UL;
            NextUInt();
            _state += seed;
            NextUInt();
        }

        private Pcg32(ulong state, ulong inc) { _state = state; _inc = inc; }

        public uint NextUInt()
        {
            ulong old = _state;
            _state = old * Mult + _inc;
            uint xorshifted = (uint)(((old >> 18) ^ old) >> 27);
            int rot = (int)(old >> 59);
            return (xorshifted >> rot) | (xorshifted << ((-rot) & 31));
        }

        public int NextInt(int maxExclusive)
        {
            if (maxExclusive <= 0) throw new ArgumentOutOfRangeException(nameof(maxExclusive));
            // Lemire 无偏界内取值
            ulong m = (ulong)(uint)NextUInt() * (ulong)(uint)maxExclusive;
            uint l = (uint)m;
            if (l < (uint)maxExclusive)
            {
                uint t = (uint)(-maxExclusive) % (uint)maxExclusive;
                while (l < t) { m = (ulong)(uint)NextUInt() * (ulong)(uint)maxExclusive; l = (uint)m; }
            }
            return (int)(m >> 32);
        }

        public int NextInclusive(int minInclusive, int maxInclusive)
        {
            if (maxInclusive < minInclusive) throw new ArgumentException("max<min");
            return minInclusive + NextInt(maxInclusive - minInclusive + 1);
        }

        public ulong[] GetState() => new[] { _state, _inc };
        public void SetState(ulong[] state) { _state = state[0]; _inc = state[1]; } // 恢复完整状态

        public IRandom Split(ulong streamId)
        {
            // 用当前状态 + streamId 派生新独立流（确定性）
            ulong s = _state ^ (streamId * Mult);
            return new Pcg32(s, (streamId << 1) | 1UL);
        }
    }
}
```

- [ ] **Step 4: 运行 → 回填黄金向量 → 通过**

Run: `dotnet test --filter Pcg32Tests`
First run: `Fixed_vector` 可能 FAIL 并打印实际值。把实际三值回填到 `golden`，重跑。
Expected: PASS（3 passed）。`SetState` 仅恢复 `_state`（`_inc` 由构造固定，往返一致）。

- [ ] **Step 5: 修复 SmokeTest 引用并 Commit**

把 `SmokeTest.cs` 改回断言 `typeof(Jianghu.Random.Pcg32).Assembly`。Run `dotnet test`，Expected PASS。
```bash
git add src/Jianghu.Core/Random tests/Jianghu.Core.Tests/Pcg32Tests.cs tests/Jianghu.Core.Tests/SmokeTest.cs
git commit -m "feat: 确定性 Pcg32 PRNG + 固定向量/状态往返/Split 测试 (R-NF1)"
```

---

### Task 2: 随机扩展（NextInclusive 端点等概率 + Fisher-Yates）

**Files:**
- Create: `src/Jianghu.Core/Random/RandomExtensions.cs`
- Test: `tests/Jianghu.Core.Tests/RandomExtensionsTests.cs`

- [ ] **Step 1: 写失败测试**

Create `tests/Jianghu.Core.Tests/RandomExtensionsTests.cs`:
```csharp
using System.Collections.Generic;
using System.Linq;
using Jianghu.Random;
using Xunit;

public class RandomExtensionsTests
{
    [Fact]
    public void Shuffle_is_deterministic_and_a_permutation()
    {
        var a = Enumerable.Range(0, 20).ToList();
        var b = Enumerable.Range(0, 20).ToList();
        new Pcg32(5, 1).Shuffle(a);
        new Pcg32(5, 1).Shuffle(b);
        Assert.Equal(a, b);                          // 同种子同序
        Assert.Equal(Enumerable.Range(0, 20), a.OrderBy(x => x)); // 是个排列
    }

    [Fact]
    public void NextInclusive_hits_both_endpoints()
    {
        var r = new Pcg32(9, 1);
        var seen = new HashSet<int>();
        for (int i = 0; i < 2000; i++) seen.Add(r.NextInclusive(3, 6));
        Assert.True(seen.SetEquals(new[] { 3, 4, 5, 6 })); // 含端点、不越界
    }
}
```

- [ ] **Step 2: 运行验证失败** — Run: `dotnet test --filter RandomExtensionsTests` → 编译失败（`Shuffle` 不存在）。

- [ ] **Step 3: 实现**

Create `src/Jianghu.Core/Random/RandomExtensions.cs`:
```csharp
using System.Collections.Generic;

namespace Jianghu.Random
{
    public static class RandomExtensions
    {
        /// <summary>Fisher–Yates 原地洗牌，确定性（用注入 IRandom）。</summary>
        public static void Shuffle<T>(this IRandom rng, IList<T> list)
        {
            for (int i = list.Count - 1; i > 0; i--)
            {
                int j = rng.NextInt(i + 1);
                (list[i], list[j]) = (list[j], list[i]);
            }
        }
    }
}
```

- [ ] **Step 4: 运行通过** — Run: `dotnet test --filter RandomExtensionsTests` → PASS (2 passed).

- [ ] **Step 5: Commit**
```bash
git add src/Jianghu.Core/Random/RandomExtensions.cs tests/Jianghu.Core.Tests/RandomExtensionsTests.cs
git commit -m "feat: Fisher-Yates Shuffle + NextInclusive 端点测试 (R-F4/R-NF6)"
```

---

### Task 3: LimitsConfig + 可行域断言 (R-NF7 / §5.3)

**Files:**
- Create: `src/Jianghu.Core/Config/LimitsConfig.cs`
- Test: `tests/Jianghu.Core.Tests/LimitsConfigTests.cs`

- [ ] **Step 1: 写失败测试**
```csharp
using System;
using Jianghu.Config;
using Xunit;

public class LimitsConfigTests
{
    [Fact]
    public void Default_is_feasible() => LimitsConfig.Default.Validate(); // 不抛

    [Fact]
    public void Infeasible_when_cap_times_count_below_sum()
    {
        var bad = LimitsConfig.Default with { StatCap = 10, StatSum = 80, StatCount = 4 }; // 4*10<80
        Assert.Throws<InvalidOperationException>(() => bad.Validate());
    }

    [Fact]
    public void Infeasible_when_min_sum_exceeds_sum()
    {
        var bad = LimitsConfig.Default with { StatMin = 25, StatSum = 80, StatCount = 4 }; // 4*25>80
        Assert.Throws<InvalidOperationException>(() => bad.Validate());
    }
}
```
Create `tests/Jianghu.Core.Tests/LimitsConfigTests.cs` with the above.

- [ ] **Step 2: 运行失败** — `dotnet test --filter LimitsConfigTests` → 编译失败。

- [ ] **Step 3: 实现**

Create `src/Jianghu.Core/Config/LimitsConfig.cs`:
```csharp
using System;

namespace Jianghu.Config
{
    public sealed record LimitsConfig
    {
        // 属性（§5）
        public int StatCount { get; init; } = 4;
        public int StatSum { get; init; } = 80;
        public int StatCap { get; init; } = 30;
        public int StatMin { get; init; } = 5;
        public int Concentration { get; init; } = 6;   // 偏中庸集中度（越大越向均值聚集）

        // 生命周期（§7.4）
        public long LifespanMin { get; init; } = 600;  // 逻辑时间单位
        public long LifespanMax { get; init; } = 1200;
        public int PopulationLow { get; init; } = 5;    // 低于则涌现新人
        public int PopulationHigh { get; init; } = 30;  // 高于则抑制涌现

        // 行动（§8）
        public int TrainGainMin { get; init; } = 1;
        public int TrainGainMax { get; init; } = 3;
        public long ActionIntervalBase { get; init; } = 10;

        public static LimitsConfig Default { get; } = new();

        /// <summary>加载期可行域非空断言（§5.3）。</summary>
        public void Validate()
        {
            if (StatCount <= 0) throw new InvalidOperationException("StatCount<=0");
            if ((long)StatCap * StatCount < StatSum)
                throw new InvalidOperationException($"可行域空: cap*count({StatCap}*{StatCount}) < sum({StatSum})");
            if ((long)StatMin * StatCount > StatSum)
                throw new InvalidOperationException($"可行域空: min*count({StatMin}*{StatCount}) > sum({StatSum})");
            if (StatMin < 0 || StatMin > StatCap) throw new InvalidOperationException("min 越界 [0,cap]");
            if (PopulationLow > PopulationHigh) throw new InvalidOperationException("人口带非法");
            if (LifespanMin > LifespanMax) throw new InvalidOperationException("寿命带非法");
        }
    }
}
```

- [ ] **Step 4: 运行通过** — `dotnet test --filter LimitsConfigTests` → PASS (3 passed).

- [ ] **Step 5: Commit**
```bash
git add src/Jianghu.Core/Config tests/Jianghu.Core.Tests/LimitsConfigTests.cs
git commit -m "feat: LimitsConfig + 可行域非空断言 (R-NF7)"
```

---

### Task 4: StatKind + StatBlock 钳制 chokepoint (R-NF7 / §5.4 / §4.6)

**Files:**
- Create: `src/Jianghu.Core/Stats/StatKind.cs`, `src/Jianghu.Core/Stats/StatBlock.cs`
- Test: `tests/Jianghu.Core.Tests/StatBlockTests.cs`

- [ ] **Step 1: 写失败测试**
```csharp
using Jianghu.Config;
using Jianghu.Stats;
using Xunit;

public class StatBlockTests
{
    [Fact]
    public void Apply_clamps_to_cap_and_floor_zero()
    {
        var c = LimitsConfig.Default; // cap 30
        var s = new StatBlock(new[] { 29, 10, 10, 10 });
        s.Apply(StatKind.Force, +5, c);
        Assert.Equal(30, s.Get(StatKind.Force));   // 越 cap 被钳
        s.Apply(StatKind.Internal, -100, c);
        Assert.Equal(0, s.Get(StatKind.Internal)); // 不为负
    }

    [Fact]
    public void Sum_reflects_values()
    {
        var s = new StatBlock(new[] { 20, 20, 20, 20 });
        Assert.Equal(80, s.Sum);
    }
}
```

- [ ] **Step 2: 运行失败** — `dotnet test --filter StatBlockTests` → 编译失败。

- [ ] **Step 3: 实现**

Create `src/Jianghu.Core/Stats/StatKind.cs`:
```csharp
namespace Jianghu.Stats
{
    public enum StatKind { Force = 0, Internal = 1, Constitution = 2, Insight = 3 } // 武力/内力/根骨/悟性
}
```
Create `src/Jianghu.Core/Stats/StatBlock.cs`:
```csharp
using System;
using Jianghu.Config;

namespace Jianghu.Stats
{
    /// <summary>属性容器。运行期变更只经 Apply（单一 chokepoint，§4.6）。</summary>
    public sealed class StatBlock
    {
        private readonly int[] _v;
        public StatBlock(int[] values)
        {
            if (values.Length != 4) throw new ArgumentException("需 4 维");
            _v = (int[])values.Clone();
        }
        public int Get(StatKind k) => _v[(int)k];
        public int Sum { get { int s = 0; foreach (var x in _v) s += x; return s; } }

        /// <summary>运行期成长：只受单项 cap，钳制到 [0, StatCap]，溢出丢弃（§5.4）。</summary>
        public void Apply(StatKind k, int delta, LimitsConfig limits)
        {
            int nv = _v[(int)k] + delta;
            if (nv < 0) nv = 0;
            if (nv > limits.StatCap) nv = limits.StatCap;
            _v[(int)k] = nv;
        }

        public StatBlock Clone() => new StatBlock(_v);
        public int[] ToArray() => (int[])_v.Clone();
    }
}
```

- [ ] **Step 4: 运行通过** — `dotnet test --filter StatBlockTests` → PASS (2 passed).

- [ ] **Step 5: Commit**
```bash
git add src/Jianghu.Core/Stats/StatKind.cs src/Jianghu.Core/Stats/StatBlock.cs tests/Jianghu.Core.Tests/StatBlockTests.cs
git commit -m "feat: StatBlock 单一钳制 chokepoint (R-NF7)"
```

---

### Task 5: 属性生成器（偏中庸、定和+封顶、无拒绝）(R-F1 / R-NF6 / §5.2)

**Files:**
- Create: `src/Jianghu.Core/Stats/StatGenerator.cs`
- Test: `tests/Jianghu.Core.Tests/StatGeneratorTests.cs`

- [ ] **Step 1: 写失败测试（不变量 + 偏中庸形状）**
```csharp
using Jianghu.Config;
using Jianghu.Random;
using Jianghu.Stats;
using Xunit;

public class StatGeneratorTests
{
    [Fact]
    public void Always_satisfies_sum_cap_min_invariants()
    {
        var c = LimitsConfig.Default;
        var r = new Pcg32(123, 1);
        for (int i = 0; i < 5000; i++)
        {
            var s = StatGenerator.Generate(r, c);
            Assert.Equal(c.StatSum, s.Sum);
            for (int k = 0; k < 4; k++)
            {
                int v = s.Get((StatKind)k);
                Assert.InRange(v, c.StatMin, c.StatCap);
            }
        }
    }

    [Fact]
    public void Distribution_is_central_tendency_not_uniform()
    {
        // 偏中庸：靠近均值(S/StatCount=20)的样本应远多于极端样本
        var c = LimitsConfig.Default;
        var r = new Pcg32(7, 1);
        int near = 0, extreme = 0;
        for (int i = 0; i < 5000; i++)
        {
            int force = StatGenerator.Generate(r, c).Get(StatKind.Force);
            if (force >= 16 && force <= 24) near++;       // |x-20|<=4
            if (force <= 8 || force >= 28) extreme++;      // 远端
        }
        Assert.True(near > extreme * 3, $"near={near} extreme={extreme} 不够中庸");
    }

    [Fact]
    public void Generation_is_deterministic()
    {
        var c = LimitsConfig.Default;
        Assert.Equal(StatGenerator.Generate(new Pcg32(42, 1), c).ToArray(),
                     StatGenerator.Generate(new Pcg32(42, 1), c).ToArray());
    }
}
```

- [ ] **Step 2: 运行失败** — `dotnet test --filter StatGeneratorTests` → 编译失败。

- [ ] **Step 3: 实现（集中度采样 + 最大余数法整数化 + 钳制修正到精确和）**

Create `src/Jianghu.Core/Stats/StatGenerator.cs`:
```csharp
using System;
using Jianghu.Config;
using Jianghu.Random;

namespace Jianghu.Stats
{
    /// <summary>偏中庸、定和+封顶、无拒绝采样（§5.2）。</summary>
    public static class StatGenerator
    {
        public static StatBlock Generate(IRandom rng, LimitsConfig c)
        {
            int n = c.StatCount;
            // 1) 集中度采样：每维取 Concentration 个 [0,1) 均值 → 趋向 0.5（中心极限→中庸）
            double[] w = new double[n];
            double total = 0;
            for (int i = 0; i < n; i++)
            {
                double acc = 0;
                for (int j = 0; j < c.Concentration; j++)
                    acc += rng.NextUInt() / 4294967296.0; // [0,1)
                w[i] = acc / c.Concentration;             // 趋中
                total += w[i];
            }
            // 2) 分配可分配预算 = Sum - n*Min 到各维，按权重；最大余数法整数化到精确和
            int budget = c.StatSum - n * c.StatMin;
            double[] raw = new double[n];
            int[] floor = new int[n];
            int assigned = 0;
            for (int i = 0; i < n; i++)
            {
                raw[i] = budget * (w[i] / total);
                floor[i] = (int)Math.Floor(raw[i]);
                assigned += floor[i];
            }
            int remainder = budget - assigned;
            // 余数给小数部分最大者（确定性：并列按索引）
            var order = new int[n];
            for (int i = 0; i < n; i++) order[i] = i;
            Array.Sort(order, (a, b) =>
            {
                double fa = raw[a] - Math.Floor(raw[a]);
                double fb = raw[b] - Math.Floor(raw[b]);
                int cmp = fb.CompareTo(fa);
                return cmp != 0 ? cmp : a.CompareTo(b);
            });
            int[] v = new int[n];
            for (int i = 0; i < n; i++) v[i] = c.StatMin + floor[i];
            for (int i = 0; i < remainder; i++) v[order[i]]++;
            // 3) 封顶修正：越 cap 的溢出回流到未满维（保持总和）
            ClampToCapKeepingSum(v, c);
            return new StatBlock(v);
        }

        private static void ClampToCapKeepingSum(int[] v, LimitsConfig c)
        {
            int overflow = 0;
            for (int i = 0; i < v.Length; i++)
                if (v[i] > c.StatCap) { overflow += v[i] - c.StatCap; v[i] = c.StatCap; }
            // 回流到未满维（确定性按索引），可行域已保证有容量
            int guard = 0;
            while (overflow > 0 && guard++ < 10000)
                for (int i = 0; i < v.Length && overflow > 0; i++)
                    if (v[i] < c.StatCap) { v[i]++; overflow--; }
        }
    }
}
```

- [ ] **Step 4: 运行通过** — `dotnet test --filter StatGeneratorTests` → PASS (3 passed)。若 `Distribution_is_central_tendency` 边际，调高 `Concentration` 默认值。

- [ ] **Step 5: Commit**
```bash
git add src/Jianghu.Core/Stats/StatGenerator.cs tests/Jianghu.Core.Tests/StatGeneratorTests.cs
git commit -m "feat: 偏中庸定和封顶无拒绝属性生成器 (R-F1/R-NF6)"
```

---

### Task 6: 身份与背景值对象（CharacterId/Persona/Goal/NodeId）

**Files:**
- Create: `src/Jianghu.Core/Model/CharacterId.cs`, `Model/NodeId.cs`, `Model/Persona.cs`, `Model/Goal.cs`
- Test: `tests/Jianghu.Core.Tests/IdentityTests.cs`

- [ ] **Step 1: 写失败测试**
```csharp
using Jianghu.Model;
using Xunit;

public class IdentityTests
{
    [Fact]
    public void CharacterId_value_equality()
    {
        Assert.Equal(new CharacterId(7), new CharacterId(7));
        Assert.NotEqual(new CharacterId(7), new CharacterId(8));
    }

    [Fact]
    public void Goal_progress_advances()
    {
        var g = new Goal(GoalKind.Advance, 0);
        var g2 = g with { Progress = g.Progress + 1 };
        Assert.Equal(1, g2.Progress);
    }
}
```

- [ ] **Step 2: 运行失败** — `dotnet test --filter IdentityTests` → 编译失败。

- [ ] **Step 3: 实现**

Create `src/Jianghu.Core/Model/CharacterId.cs`:
```csharp
namespace Jianghu.Model
{
    public readonly record struct CharacterId(long Value); // 不可变主键（§4.5）
}
```
Create `src/Jianghu.Core/Model/NodeId.cs`:
```csharp
namespace Jianghu.Model
{
    public readonly record struct NodeId(int Value);
}
```
Create `src/Jianghu.Core/Model/Persona.cs`:
```csharp
namespace Jianghu.Model
{
    public enum ArchetypeKind { Martial, Knight } // 武痴(重修炼/切磋) / 游侠(重游历/结识)
    public sealed record Persona(string Name, string Title, string Origin, ArchetypeKind Archetype, int? SectId);
}
```
Create `src/Jianghu.Core/Model/Goal.cs`:
```csharp
namespace Jianghu.Model
{
    public enum GoalKind { Advance, Wander } // 精进武学 / 游历访友
    public sealed record Goal(GoalKind Kind, int Progress);
}
```

- [ ] **Step 4: 运行通过** — `dotnet test --filter IdentityTests` → PASS (2 passed).

- [ ] **Step 5: Commit**
```bash
git add src/Jianghu.Core/Model/CharacterId.cs src/Jianghu.Core/Model/NodeId.cs src/Jianghu.Core/Model/Persona.cs src/Jianghu.Core/Model/Goal.cs tests/Jianghu.Core.Tests/IdentityTests.cs
git commit -m "feat: CharacterId 主键 + Persona/Goal/NodeId 值对象 (R-F1)"
```

---

### Task 7: 记忆与关系（隔离记忆 + 有向好感图）

**Files:**
- Create: `src/Jianghu.Core/Model/MemoryEntry.cs`, `Model/MemoryStore.cs`, `Model/Relations.cs`
- Test: `tests/Jianghu.Core.Tests/MemoryRelationsTests.cs`

- [ ] **Step 1: 写失败测试**
```csharp
using System.Linq;
using Jianghu.Model;
using Xunit;

public class MemoryRelationsTests
{
    [Fact]
    public void Memory_caps_keeping_most_salient()
    {
        var m = new MemoryStore(cap: 3);
        m.Remember(new MemoryEntry(1, "spar", new CharacterId(1), new CharacterId(2), valence: 1));
        m.Remember(new MemoryEntry(2, "spar", new CharacterId(1), new CharacterId(3), valence: 9)); // 高显著
        m.Remember(new MemoryEntry(3, "train", new CharacterId(1), null, valence: 1));
        m.Remember(new MemoryEntry(4, "train", new CharacterId(1), null, valence: 1)); // 触发淘汰
        Assert.Equal(3, m.Recall().Count);
        Assert.Contains(m.Recall(), e => e.Valence == 9); // 高显著保留
    }

    [Fact]
    public void Relations_directed_affinity_adjust_and_clamp()
    {
        var r = new Relations();
        var a = new CharacterId(1); var b = new CharacterId(2);
        r.Adjust(a, b, +30);
        Assert.Equal(30, r.Affinity(a, b));
        Assert.Equal(0, r.Affinity(b, a));      // 有向
        r.Adjust(a, b, +1000);
        Assert.Equal(100, r.Affinity(a, b));    // 钳到 [-100,100]
    }
}
```

- [ ] **Step 2: 运行失败** — `dotnet test --filter MemoryRelationsTests` → 编译失败。

- [ ] **Step 3: 实现**

Create `src/Jianghu.Core/Model/MemoryEntry.cs`:
```csharp
namespace Jianghu.Model
{
    public readonly record struct MemoryEntry(long Tick, string Kind, CharacterId Subject, CharacterId? Object, int Valence);
}
```
Create `src/Jianghu.Core/Model/MemoryStore.cs`:
```csharp
using System.Collections.Generic;
using System.Linq;

namespace Jianghu.Model
{
    /// <summary>聚合根私有记忆。超限按 |Valence| 升序淘汰，平手淘汰更早 Tick（确定性，§4.5）。</summary>
    public sealed class MemoryStore
    {
        private readonly List<MemoryEntry> _items = new List<MemoryEntry>();
        private readonly int _cap;
        public MemoryStore(int cap) { _cap = cap; }

        public void Remember(MemoryEntry e)
        {
            _items.Add(e);
            if (_items.Count <= _cap) return;
            int worst = 0;
            for (int i = 1; i < _items.Count; i++)
            {
                int wa = System.Math.Abs(_items[worst].Valence), wi = System.Math.Abs(_items[i].Valence);
                if (wi < wa || (wi == wa && _items[i].Tick < _items[worst].Tick)) worst = i;
            }
            _items.RemoveAt(worst);
        }

        public IReadOnlyList<MemoryEntry> Recall() => _items.AsReadOnly();
        public MemoryStore Clone()
        {
            var m = new MemoryStore(_cap);
            m._items.AddRange(_items);
            return m;
        }
    }
}
```
Create `src/Jianghu.Core/Model/Relations.cs`:
```csharp
using System.Collections.Generic;

namespace Jianghu.Model
{
    /// <summary>有向好感图，键 (from,to)，值钳制 [-100,100]。</summary>
    public sealed class Relations
    {
        private readonly Dictionary<(long, long), int> _aff = new Dictionary<(long, long), int>();
        public int Affinity(CharacterId from, CharacterId to)
            => _aff.TryGetValue((from.Value, to.Value), out var v) ? v : 0;

        public int Adjust(CharacterId from, CharacterId to, int delta)
        {
            int nv = Affinity(from, to) + delta;
            if (nv > 100) nv = 100; if (nv < -100) nv = -100;
            _aff[(from.Value, to.Value)] = nv;
            return nv;
        }

        public Relations Clone()
        {
            var r = new Relations();
            foreach (var kv in _aff) r._aff[kv.Key] = kv.Value;
            return r;
        }
    }
}
```

- [ ] **Step 4: 运行通过** — `dotnet test --filter MemoryRelationsTests` → PASS (2 passed).

- [ ] **Step 5: Commit**
```bash
git add src/Jianghu.Core/Model/MemoryEntry.cs src/Jianghu.Core/Model/MemoryStore.cs src/Jianghu.Core/Model/Relations.cs tests/Jianghu.Core.Tests/MemoryRelationsTests.cs
git commit -m "feat: 隔离记忆(显著度淘汰) + 有向好感图 (R-F3/§4.5)"
```

---

### Task 8: DomainEvent 层 + Chronicle 文本投影 (R-F3 / §4.6)

**Files:**
- Create: `src/Jianghu.Core/Events/DomainEvent.cs`, `Events/Chronicle.cs`
- Test: `tests/Jianghu.Core.Tests/ChronicleTests.cs`

- [ ] **Step 1: 写失败测试**
```csharp
using Jianghu.Events;
using Jianghu.Model;
using Jianghu.Stats;
using Xunit;

public class ChronicleTests
{
    [Fact]
    public void Projects_events_to_readable_text_deterministically()
    {
        var ch = new Chronicle();
        ch.Append(new CharacterTrained(10, new CharacterId(1), StatKind.Force, 2), n => "甲");
        ch.Append(new DuelResolved(11, new CharacterId(1), new CharacterId(2), 5), n => n.Value == 1 ? "甲" : "乙");
        Assert.Equal(2, ch.Lines.Count);
        Assert.Contains("甲", ch.Lines[0]);
        Assert.Contains("切磋", ch.Lines[1]);
    }
}
```

- [ ] **Step 2: 运行失败** — `dotnet test --filter ChronicleTests` → 编译失败。

- [ ] **Step 3: 实现**

Create `src/Jianghu.Core/Events/DomainEvent.cs`:
```csharp
using Jianghu.Model;
using Jianghu.Stats;

namespace Jianghu.Events
{
    public abstract record DomainEvent(long Tick);
    public sealed record CharacterBorn(long Tick, CharacterId Id) : DomainEvent(Tick);
    public sealed record CharacterTrained(long Tick, CharacterId Id, StatKind Stat, int Delta) : DomainEvent(Tick);
    public sealed record CharacterTraveled(long Tick, CharacterId Id, NodeId From, NodeId To) : DomainEvent(Tick);
    public sealed record DuelResolved(long Tick, CharacterId Winner, CharacterId Loser, int Margin) : DomainEvent(Tick);
    public sealed record RelationChanged(long Tick, CharacterId From, CharacterId To, int Delta, int NewValue) : DomainEvent(Tick);
    public sealed record CharacterDied(long Tick, CharacterId Id, long Age) : DomainEvent(Tick);
}
```
Create `src/Jianghu.Core/Events/Chronicle.cs`:
```csharp
using System;
using System.Collections.Generic;
using Jianghu.Model;

namespace Jianghu.Events
{
    /// <summary>DomainEvent 的只读文本投影（武侠味模板）。name 解析器把 Id 映射为称谓。</summary>
    public sealed class Chronicle
    {
        private readonly List<string> _lines = new List<string>();
        public IReadOnlyList<string> Lines => _lines.AsReadOnly();
        public int Count => _lines.Count;

        public void Append(DomainEvent e, Func<CharacterId, string> name)
        {
            string text;
            switch (e)
            {
                case CharacterBorn b: text = $"[{b.Tick}] {name(b.Id)} 踏入江湖。"; break;
                case CharacterTrained t: text = $"[{t.Tick}] {name(t.Id)} 闭关苦修，{t.Stat} 精进 {t.Delta}。"; break;
                case CharacterTraveled v: text = $"[{v.Tick}] {name(v.Id)} 自 {v.From.Value} 行至 {v.To.Value}。"; break;
                case DuelResolved d: text = $"[{d.Tick}] {name(d.Winner)} 与 {name(d.Loser)} 切磋，胜（差 {d.Margin}）。"; break;
                case RelationChanged r: text = $"[{r.Tick}] {name(r.From)} 对 {name(r.To)} 情谊变为 {r.NewValue}。"; break;
                case CharacterDied x: text = $"[{x.Tick}] {name(x.Id)} 寿尽，享年 {x.Age}，江湖再无此人。"; break;
                default: text = $"[{e.Tick}] (未知事件)"; break;
            }
            _lines.Add(text);
        }
    }
}
```

- [ ] **Step 4: 运行通过** — `dotnet test --filter ChronicleTests` → PASS (1 passed).

- [ ] **Step 5: Commit**
```bash
git add src/Jianghu.Core/Events tests/Jianghu.Core.Tests/ChronicleTests.cs
git commit -m "feat: DomainEvent 单源 + Chronicle 文本投影 (R-F3/R-F7)"
```

---

### Task 9: Character 聚合 + Sect + WorldNode

**Files:**
- Create: `src/Jianghu.Core/Model/Character.cs`, `Model/Sect.cs`, `Model/WorldNode.cs`
- Test: `tests/Jianghu.Core.Tests/CharacterTests.cs`

- [ ] **Step 1: 写失败测试**
```csharp
using Jianghu.Config;
using Jianghu.Model;
using Jianghu.Stats;
using Xunit;

public class CharacterTests
{
    [Fact]
    public void Character_exposes_memory_only_through_methods()
    {
        var ch = new Character(new CharacterId(1),
            new Persona("无名", "客", "市井", ArchetypeKind.Martial, null),
            new StatBlock(new[] { 20, 20, 20, 20 }),
            new NodeId(0), new Goal(GoalKind.Advance, 0),
            age: 0, lifespan: 800, memoryCap: 16);
        ch.Remember(new MemoryEntry(1, "x", new CharacterId(1), null, 1));
        Assert.Single(ch.RecallMemory());
        Assert.True(ch.Alive);
    }
}
```

- [ ] **Step 2: 运行失败** — `dotnet test --filter CharacterTests` → 编译失败。

- [ ] **Step 3: 实现**

Create `src/Jianghu.Core/Model/WorldNode.cs`:
```csharp
namespace Jianghu.Model
{
    public sealed record WorldNode(NodeId Id, string Name);
}
```
Create `src/Jianghu.Core/Model/Sect.cs`:
```csharp
using System.Collections.Generic;

namespace Jianghu.Model
{
    public sealed class Sect
    {
        public int Id { get; }
        public string Name { get; }
        public List<CharacterId> Disciples { get; } = new List<CharacterId>();
        public Sect(int id, string name) { Id = id; Name = name; }
    }
}
```
Create `src/Jianghu.Core/Model/Character.cs`:
```csharp
using System.Collections.Generic;
using Jianghu.Stats;

namespace Jianghu.Model
{
    /// <summary>聚合根。MemoryStore 私有，只经 Remember/RecallMemory 暴露（§4.5）。</summary>
    public sealed class Character
    {
        public CharacterId Id { get; }
        public Persona Persona { get; set; }
        public StatBlock Stats { get; }
        public NodeId Node { get; set; }
        public Goal Goal { get; set; }
        public long Age { get; set; }
        public long Lifespan { get; }
        public long NextActAt { get; set; }
        public bool Alive { get; set; } = true;
        private readonly MemoryStore _memory;

        public Character(CharacterId id, Persona persona, StatBlock stats, NodeId node, Goal goal,
                         long age, long lifespan, int memoryCap)
        {
            Id = id; Persona = persona; Stats = stats; Node = node; Goal = goal;
            Age = age; Lifespan = lifespan; _memory = new MemoryStore(memoryCap);
        }

        private Character(CharacterId id, Persona persona, StatBlock stats, NodeId node, Goal goal,
                          long age, long lifespan, long nextActAt, bool alive, MemoryStore mem)
        {
            Id = id; Persona = persona; Stats = stats; Node = node; Goal = goal;
            Age = age; Lifespan = lifespan; NextActAt = nextActAt; Alive = alive; _memory = mem;
        }

        public void Remember(MemoryEntry e) => _memory.Remember(e);
        public IReadOnlyList<MemoryEntry> RecallMemory() => _memory.Recall();

        public Character Clone() =>
            new Character(Id, Persona, Stats.Clone(), Node, Goal, Age, Lifespan, NextActAt, Alive, _memory.Clone());
    }
}
```

- [ ] **Step 4: 运行通过** — `dotnet test --filter CharacterTests` → PASS (1 passed).

- [ ] **Step 5: Commit**
```bash
git add src/Jianghu.Core/Model/Character.cs src/Jianghu.Core/Model/Sect.cs src/Jianghu.Core/Model/WorldNode.cs tests/Jianghu.Core.Tests/CharacterTests.cs
git commit -m "feat: Character 聚合根(私有记忆) + Sect + WorldNode (R-F1)"
```

---

### Task 10: 动作契约 + DecisionContext

**Files:**
- Create: `src/Jianghu.Core/Actions/ActionType.cs`, `Actions/ActionChoice.cs`, `Actions/ValidationResult.cs`, `Actions/IAction.cs`, `Decide/DecisionContext.cs`, `Decide/IBrain.cs`
- Test: `tests/Jianghu.Core.Tests/ContractsTests.cs`

- [ ] **Step 1: 写失败测试**
```csharp
using System.Collections.Generic;
using Jianghu.Actions;
using Jianghu.Decide;
using Jianghu.Model;
using Jianghu.Stats;
using Xunit;

public class ContractsTests
{
    [Fact]
    public void DecisionContext_is_immutable_snapshot()
    {
        var ctx = new DecisionContext(
            new CharacterId(1), new StatBlock(new[] { 20, 20, 20, 20 }),
            new Goal(GoalKind.Advance, 0), new NodeId(0),
            new List<NearbyActor>(), new List<ActionType> { ActionType.Train },
            new List<MemoryEntry>());
        Assert.Equal(ActionType.Train, ctx.Allowed[0]);
    }

    [Fact]
    public void ActionChoice_carries_typed_payload()
    {
        ActionChoice c = new TrainChoice(StatKind.Force);
        Assert.Equal(ActionType.Train, c.Type);
        Assert.Equal(StatKind.Force, ((TrainChoice)c).Stat);
    }
}
```

- [ ] **Step 2: 运行失败** — `dotnet test --filter ContractsTests` → 编译失败。

- [ ] **Step 3: 实现**

Create `src/Jianghu.Core/Actions/ActionType.cs`:
```csharp
namespace Jianghu.Actions
{
    public enum ActionType { Train, Travel, Spar } // 修炼/游历/切磋
}
```
Create `src/Jianghu.Core/Actions/ActionChoice.cs`:
```csharp
using Jianghu.Model;
using Jianghu.Stats;

namespace Jianghu.Actions
{
    public abstract record ActionChoice(ActionType Type);
    public sealed record TrainChoice(StatKind Stat) : ActionChoice(ActionType.Train);
    public sealed record TravelChoice(NodeId To) : ActionChoice(ActionType.Travel);
    public sealed record SparChoice(CharacterId Target) : ActionChoice(ActionType.Spar);
}
```
Create `src/Jianghu.Core/Actions/ValidationResult.cs`:
```csharp
namespace Jianghu.Actions
{
    public readonly record struct ValidationResult(bool Ok, string Reason)
    {
        public static ValidationResult Valid => new ValidationResult(true, "");
        public static ValidationResult Invalid(string reason) => new ValidationResult(false, reason);
    }
}
```
Create `src/Jianghu.Core/Decide/DecisionContext.cs`:
```csharp
using System.Collections.Generic;
using Jianghu.Actions;
using Jianghu.Model;
using Jianghu.Stats;

namespace Jianghu.Decide
{
    public readonly record struct NearbyActor(CharacterId Id, int Power, int Affinity);

    /// <summary>角色可感知子集（不含 World）→ Brain 无法越权改世界（R-NF1）。</summary>
    public sealed record DecisionContext(
        CharacterId Self,
        StatBlock Stats,
        Goal Goal,
        NodeId Node,
        IReadOnlyList<NearbyActor> Nearby,
        IReadOnlyList<ActionType> Allowed,
        IReadOnlyList<MemoryEntry> Memory);
}
```
Create `src/Jianghu.Core/Decide/IBrain.cs`:
```csharp
using System.Threading;
using System.Threading.Tasks;
using Jianghu.Actions;

namespace Jianghu.Decide
{
    public interface IBrain
    {
        ValueTask<ActionChoice> DecideAsync(DecisionContext ctx, CancellationToken ct);
    }
}
```
Create `src/Jianghu.Core/Actions/IAction.cs`:
```csharp
using System.Collections.Generic;
using Jianghu.Decide;
using Jianghu.Events;
using Jianghu.Model;

namespace Jianghu.Actions
{
    public interface IAction
    {
        ActionType Type { get; }
        ValidationResult CanExecute(IWorldView world, Character actor, DecisionContext ctx);
        IReadOnlyList<DomainEvent> Apply(IWorldMutator world, Character actor, ActionChoice choice);
    }

    /// <summary>只读世界视图（CanExecute 用）。</summary>
    public interface IWorldView
    {
        long Clock { get; }
        IReadOnlyList<Character> AtNode(NodeId node);
        int NodeCount { get; }
    }

    /// <summary>受控世界变更（Apply 用）：属性只经此改，关系经此调。</summary>
    public interface IWorldMutator : IWorldView
    {
        void ApplyStat(Character c, Jianghu.Stats.StatKind k, int delta);
        int AdjustRelation(CharacterId from, CharacterId to, int delta);
        void Move(Character c, NodeId to);
    }
}
```

- [ ] **Step 4: 运行通过** — `dotnet test --filter ContractsTests` → PASS (2 passed).

- [ ] **Step 5: Commit**
```bash
git add src/Jianghu.Core/Actions src/Jianghu.Core/Decide tests/Jianghu.Core.Tests/ContractsTests.cs
git commit -m "feat: 动作契约/ActionChoice/DecisionContext/IBrain 端口 (R-F2)"
```

---

### Task 11: 三动作 + ActionSystem 分发

**Files:**
- Create: `src/Jianghu.Core/Actions/TrainAction.cs`, `Actions/TravelAction.cs`, `Actions/SparAction.cs`, `Actions/ActionSystem.cs`
- Test: `tests/Jianghu.Core.Tests/ActionsTests.cs`（用一个内存 fake IWorldMutator）

- [ ] **Step 1: 写失败测试（含 fake world）**
```csharp
using System.Collections.Generic;
using System.Linq;
using Jianghu.Actions;
using Jianghu.Config;
using Jianghu.Events;
using Jianghu.Model;
using Jianghu.Stats;
using Xunit;

public class ActionsTests
{
    private sealed class FakeWorld : IWorldMutator
    {
        public long Clock { get; set; } = 5;
        public List<Character> All { get; } = new List<Character>();
        public Relations Rel { get; } = new Relations();
        public LimitsConfig Limits { get; } = LimitsConfig.Default;
        public int NodeCount => 3;
        public IReadOnlyList<Character> AtNode(NodeId n) => All.Where(c => c.Node.Value == n.Value && c.Alive).ToList();
        public void ApplyStat(Character c, StatKind k, int d) => c.Stats.Apply(k, d, Limits);
        public int AdjustRelation(CharacterId f, CharacterId t, int d) => Rel.Adjust(f, t, d);
        public void Move(Character c, NodeId to) => c.Node = to;
    }

    private static Character Make(long id, int node, int force) =>
        new Character(new CharacterId(id), new Persona("x", "y", "z", ArchetypeKind.Martial, null),
            new StatBlock(new[] { force, 20, 20, 20 }), new NodeId(node), new Goal(GoalKind.Advance, 0), 0, 800, 16);

    [Fact]
    public void Train_raises_stat_and_emits_event()
    {
        var w = new FakeWorld(); var a = Make(1, 0, 20); w.All.Add(a);
        var evs = new ActionSystem(w.Limits).Execute(w, a, new TrainChoice(StatKind.Force));
        Assert.True(a.Stats.Get(StatKind.Force) > 20);
        Assert.Single(evs.OfType<CharacterTrained>());
    }

    [Fact]
    public void Spar_changes_relations_and_emits_duel()
    {
        var w = new FakeWorld();
        var a = Make(1, 0, 25); var b = Make(2, 0, 20); w.All.Add(a); w.All.Add(b);
        var evs = new ActionSystem(w.Limits).Execute(w, a, new SparChoice(new CharacterId(2)));
        Assert.Single(evs.OfType<DuelResolved>());
        Assert.True(evs.OfType<RelationChanged>().Any());
    }

    [Fact]
    public void Travel_moves_and_emits()
    {
        var w = new FakeWorld(); var a = Make(1, 0, 20); w.All.Add(a);
        var evs = new ActionSystem(w.Limits).Execute(w, a, new TravelChoice(new NodeId(2)));
        Assert.Equal(2, a.Node.Value);
        Assert.Single(evs.OfType<CharacterTraveled>());
    }
}
```

- [ ] **Step 2: 运行失败** — `dotnet test --filter ActionsTests` → 编译失败。

- [ ] **Step 3: 实现三动作 + ActionSystem**

Create `src/Jianghu.Core/Actions/TrainAction.cs`:
```csharp
using System.Collections.Generic;
using Jianghu.Config;
using Jianghu.Decide;
using Jianghu.Events;
using Jianghu.Model;
using Jianghu.Stats;

namespace Jianghu.Actions
{
    public sealed class TrainAction : IAction
    {
        private readonly LimitsConfig _c;
        public TrainAction(LimitsConfig c) { _c = c; }
        public ActionType Type => ActionType.Train;
        public ValidationResult CanExecute(IWorldView w, Character a, DecisionContext ctx) => ValidationResult.Valid;

        public IReadOnlyList<DomainEvent> Apply(IWorldMutator w, Character a, ActionChoice choice)
        {
            var c = (TrainChoice)choice;
            int gain = _c.TrainGainMin + (a.Stats.Get(StatKind.Insight) * (_c.TrainGainMax - _c.TrainGainMin)) / 30;
            if (gain < _c.TrainGainMin) gain = _c.TrainGainMin;
            int before = a.Stats.Get(c.Stat);
            w.ApplyStat(a, c.Stat, gain);
            int delta = a.Stats.Get(c.Stat) - before;
            return new DomainEvent[] { new CharacterTrained(w.Clock, a.Id, c.Stat, delta) };
        }
    }
}
```
Create `src/Jianghu.Core/Actions/TravelAction.cs`:
```csharp
using System.Collections.Generic;
using Jianghu.Decide;
using Jianghu.Events;
using Jianghu.Model;

namespace Jianghu.Actions
{
    public sealed class TravelAction : IAction
    {
        public ActionType Type => ActionType.Travel;
        public ValidationResult CanExecute(IWorldView w, Character a, DecisionContext ctx)
        {
            var c = (TravelChoice)null; // 占位避免警告
            return ValidationResult.Valid;
        }
        public IReadOnlyList<DomainEvent> Apply(IWorldMutator w, Character a, ActionChoice choice)
        {
            var c = (TravelChoice)choice;
            var from = a.Node;
            if (c.To.Value < 0 || c.To.Value >= w.NodeCount) return new DomainEvent[0];
            w.Move(a, c.To);
            return new DomainEvent[] { new CharacterTraveled(w.Clock, a.Id, from, c.To) };
        }
    }
}
```
Create `src/Jianghu.Core/Actions/SparAction.cs`:
```csharp
using System.Collections.Generic;
using Jianghu.Decide;
using Jianghu.Events;
using Jianghu.Model;
using Jianghu.Stats;

namespace Jianghu.Actions
{
    public sealed class SparAction : IAction
    {
        public ActionType Type => ActionType.Spar;

        public ValidationResult CanExecute(IWorldView w, Character a, DecisionContext ctx)
        {
            var c = (SparChoice)null;
            return ValidationResult.Valid;
        }

        private static int Power(Character c) =>
            c.Stats.Get(StatKind.Force) * 2 + c.Stats.Get(StatKind.Internal) + c.Stats.Get(StatKind.Constitution);

        public IReadOnlyList<DomainEvent> Apply(IWorldMutator w, Character a, ActionChoice choice)
        {
            var c = (SparChoice)choice;
            Character target = null;
            foreach (var x in w.AtNode(a.Node)) if (x.Id.Value == c.Target.Value) { target = x; break; }
            if (target == null) return new DomainEvent[0];

            int pa = Power(a), pb = Power(target);
            var winner = pa >= pb ? a : target;
            var loser = pa >= pb ? target : a;
            int margin = System.Math.Abs(pa - pb);

            int wToL = w.AdjustRelation(winner.Id, loser.Id, +3);   // 胜者欣赏
            int lToW = w.AdjustRelation(loser.Id, winner.Id, margin > 20 ? -4 : +2); // 大败记恨/小败敬服
            return new DomainEvent[]
            {
                new DuelResolved(w.Clock, winner.Id, loser.Id, margin),
                new RelationChanged(w.Clock, winner.Id, loser.Id, +3, wToL),
                new RelationChanged(w.Clock, loser.Id, winner.Id, margin > 20 ? -4 : +2, lToW),
            };
        }
    }
}
```
Create `src/Jianghu.Core/Actions/ActionSystem.cs`:
```csharp
using System.Collections.Generic;
using Jianghu.Config;
using Jianghu.Events;
using Jianghu.Model;

namespace Jianghu.Actions
{
    /// <summary>按 ActionType 分发到 IAction.Apply。属性只经 IWorldMutator 改（统一钳制）。</summary>
    public sealed class ActionSystem
    {
        private readonly Dictionary<ActionType, IAction> _actions;
        public ActionSystem(LimitsConfig c)
        {
            _actions = new Dictionary<ActionType, IAction>
            {
                { ActionType.Train, new TrainAction(c) },
                { ActionType.Travel, new TravelAction() },
                { ActionType.Spar, new SparAction() },
            };
        }
        public IReadOnlyList<ActionType> Types => new List<ActionType>(_actions.Keys);

        public IReadOnlyList<DomainEvent> Execute(IWorldMutator w, Character actor, ActionChoice choice)
            => _actions[choice.Type].Apply(w, actor, choice);
    }
}
```

- [ ] **Step 4: 运行通过** — `dotnet test --filter ActionsTests` → PASS (3 passed).

- [ ] **Step 5: Commit**
```bash
git add src/Jianghu.Core/Actions/TrainAction.cs src/Jianghu.Core/Actions/TravelAction.cs src/Jianghu.Core/Actions/SparAction.cs src/Jianghu.Core/Actions/ActionSystem.cs tests/Jianghu.Core.Tests/ActionsTests.cs
git commit -m "feat: 修炼/游历/切磋 三动作 + ActionSystem 分发 (R-F2/R-F3)"
```

---

### Task 12: RuleBrain 目标驱动效用机 (§7.3)

**Files:**
- Create: `src/Jianghu.Core/Decide/RuleBrain.cs`
- Test: `tests/Jianghu.Core.Tests/RuleBrainTests.cs`

- [ ] **Step 1: 写失败测试**
```csharp
using System.Collections.Generic;
using System.Threading;
using Jianghu.Actions;
using Jianghu.Decide;
using Jianghu.Model;
using Jianghu.Random;
using Jianghu.Stats;
using Xunit;

public class RuleBrainTests
{
    private static DecisionContext Ctx(GoalKind goal, ArchetypeKind arch, List<NearbyActor> nearby, List<ActionType> allowed)
        => new DecisionContext(new CharacterId(1), new StatBlock(new[] { 10, 10, 10, 20 }),
            new Goal(goal, 0), new NodeId(0), nearby, allowed, new List<MemoryEntry>());

    [Fact]
    public void Advance_goal_with_no_neighbors_prefers_train()
    {
        var brain = new RuleBrain(new Pcg32(1, 1), ArchetypeKind.Martial);
        var ctx = Ctx(GoalKind.Advance, ArchetypeKind.Martial, new List<NearbyActor>(),
            new List<ActionType> { ActionType.Train, ActionType.Travel, ActionType.Spar });
        var choice = brain.DecideAsync(ctx, CancellationToken.None).GetAwaiter().GetResult();
        Assert.Equal(ActionType.Train, choice.Type);
    }

    [Fact]
    public void Repeat_decay_breaks_self_similarity()
    {
        var brain = new RuleBrain(new Pcg32(1, 1), ArchetypeKind.Martial);
        var ctx = Ctx(GoalKind.Advance, ArchetypeKind.Martial,
            new List<NearbyActor> { new NearbyActor(new CharacterId(2), 40, 0) },
            new List<ActionType> { ActionType.Train, ActionType.Travel, ActionType.Spar });
        var seen = new HashSet<ActionType>();
        for (int i = 0; i < 8; i++) seen.Add(brain.DecideAsync(ctx, CancellationToken.None).GetAwaiter().GetResult().Type);
        Assert.True(seen.Count >= 2, "重复衰减应打破单一动作");
    }
}
```

- [ ] **Step 2: 运行失败** — `dotnet test --filter RuleBrainTests` → 编译失败。

- [ ] **Step 3: 实现**

Create `src/Jianghu.Core/Decide/RuleBrain.cs`:
```csharp
using System.Collections.Generic;
using System.Threading;
using System.Threading.Tasks;
using Jianghu.Actions;
using Jianghu.Model;
using Jianghu.Random;
using Jianghu.Stats;

namespace Jianghu.Decide
{
    /// <summary>目标驱动效用机（§7.3）：消费目标/关系梯度/机会窗口/原型偏好/重复衰减。</summary>
    public sealed class RuleBrain : IBrain
    {
        private readonly IRandom _rng;
        private readonly ArchetypeKind _arch;
        private ActionType _last;
        private int _streak;

        public RuleBrain(IRandom rng, ArchetypeKind arch) { _rng = rng; _arch = arch; }

        public ValueTask<ActionChoice> DecideAsync(DecisionContext ctx, CancellationToken ct)
        {
            ActionType best = ActionType.Train; double bestU = double.NegativeInfinity; ActionChoice bestChoice = null;
            foreach (var act in ctx.Allowed)
            {
                var (u, choice) = Evaluate(act, ctx);
                if (act == _last) u *= System.Math.Pow(0.5, _streak); // 重复衰减
                u += _rng.NextInt(100) / 1000.0;                       // 确定性打破平手
                if (u > bestU) { bestU = u; best = act; bestChoice = choice; }
            }
            _streak = best == _last ? _streak + 1 : 0;
            _last = best;
            return new ValueTask<ActionChoice>(bestChoice);
        }

        private (double, ActionChoice) Evaluate(ActionType act, DecisionContext ctx)
        {
            double wArch = _arch == ArchetypeKind.Martial ? 1.0 : 0.5;
            switch (act)
            {
                case ActionType.Train:
                {
                    var stat = WeakestCombat(ctx.Stats);
                    double goalW = ctx.Goal.Kind == GoalKind.Advance ? 1.5 : 0.6;
                    double headroom = 1.0 - ctx.Stats.Get(stat) / 30.0;
                    return (goalW * wArch * (0.5 + headroom), new TrainChoice(stat));
                }
                case ActionType.Travel:
                {
                    double goalW = ctx.Goal.Kind == GoalKind.Wander ? 1.4 : 0.7;
                    double lonely = ctx.Nearby.Count == 0 ? 1.0 : 0.3;
                    int to = (ctx.Node.Value + 1); // 简单巡游
                    return (goalW * lonely, new TravelChoice(new NodeId(to)));
                }
                case ActionType.Spar:
                {
                    if (ctx.Nearby.Count == 0) return (double.NegativeInfinity, new SparChoice(new CharacterId(-1)));
                    var t = BestSparTarget(ctx);
                    double rival = (40 - System.Math.Abs(SelfPower(ctx.Stats) - t.Power)) / 40.0; // 势均力敌更想切磋
                    double notFoe = t.Affinity > -50 ? 1.0 : 0.2;
                    return (wArch * (0.6 + rival) * notFoe, new SparChoice(t.Id));
                }
                default: return (double.NegativeInfinity, new TrainChoice(StatKind.Force));
            }
        }

        private static int SelfPower(StatBlock s) =>
            s.Get(StatKind.Force) * 2 + s.Get(StatKind.Internal) + s.Get(StatKind.Constitution);

        private static StatKind WeakestCombat(StatBlock s)
        {
            StatKind w = StatKind.Force; int min = int.MaxValue;
            foreach (var k in new[] { StatKind.Force, StatKind.Internal, StatKind.Constitution })
                if (s.Get(k) < min) { min = s.Get(k); w = k; }
            return w;
        }

        private static NearbyActor BestSparTarget(DecisionContext ctx)
        {
            NearbyActor best = ctx.Nearby[0]; int bestGap = int.MaxValue; int self = SelfPower(ctx.Stats);
            foreach (var n in ctx.Nearby)
            {
                int gap = System.Math.Abs(self - n.Power);
                if (gap < bestGap) { bestGap = gap; best = n; }
            }
            return best;
        }
    }
}
```

- [ ] **Step 4: 运行通过** — `dotnet test --filter RuleBrainTests` → PASS (2 passed)。

- [ ] **Step 5: Commit**
```bash
git add src/Jianghu.Core/Decide/RuleBrain.cs tests/Jianghu.Core.Tests/RuleBrainTests.cs
git commit -m "feat: RuleBrain 目标驱动效用机(关系梯度/机会/重复衰减) (§7.3)"
```

---

### Task 13: Scheduler 最小堆 (R-NF3 / §7.1)

**Files:**
- Create: `src/Jianghu.Core/Sim/Scheduler.cs`
- Test: `tests/Jianghu.Core.Tests/SchedulerTests.cs`

- [ ] **Step 1: 写失败测试**
```csharp
using Jianghu.Model;
using Jianghu.Sim;
using Xunit;

public class SchedulerTests
{
    [Fact]
    public void Pops_in_time_then_id_order()
    {
        var s = new Scheduler();
        s.Push(new CharacterId(2), at: 10);
        s.Push(new CharacterId(1), at: 10);  // 同时刻 → 小 Id 先
        s.Push(new CharacterId(3), at: 5);
        Assert.Equal(new CharacterId(3), s.PopMin().Id);
        Assert.Equal(new CharacterId(1), s.PopMin().Id);
        Assert.Equal(new CharacterId(2), s.PopMin().Id);
        Assert.True(s.IsEmpty);
    }
}
```

- [ ] **Step 2: 运行失败** — `dotnet test --filter SchedulerTests` → 编译失败。

- [ ] **Step 3: 实现（二叉最小堆，键 (at,id)）**

Create `src/Jianghu.Core/Sim/Scheduler.cs`:
```csharp
using System;
using System.Collections.Generic;
using Jianghu.Model;

namespace Jianghu.Sim
{
    public readonly record struct ScheduleItem(CharacterId Id, long At);

    /// <summary>确定性最小堆，按 (At, Id) 排序。</summary>
    public sealed class Scheduler
    {
        private readonly List<ScheduleItem> _h = new List<ScheduleItem>();
        public bool IsEmpty => _h.Count == 0;
        public int Count => _h.Count;

        private static int Cmp(ScheduleItem a, ScheduleItem b)
        {
            int c = a.At.CompareTo(b.At);
            return c != 0 ? c : a.Id.Value.CompareTo(b.Id.Value);
        }

        public void Push(CharacterId id, long at)
        {
            _h.Add(new ScheduleItem(id, at));
            int i = _h.Count - 1;
            while (i > 0) { int p = (i - 1) / 2; if (Cmp(_h[i], _h[p]) < 0) { (_h[i], _h[p]) = (_h[p], _h[i]); i = p; } else break; }
        }

        public ScheduleItem PopMin()
        {
            if (_h.Count == 0) throw new InvalidOperationException("empty");
            var min = _h[0];
            _h[0] = _h[_h.Count - 1];
            _h.RemoveAt(_h.Count - 1);
            int i = 0;
            while (true)
            {
                int l = 2 * i + 1, r = 2 * i + 2, s = i;
                if (l < _h.Count && Cmp(_h[l], _h[s]) < 0) s = l;
                if (r < _h.Count && Cmp(_h[r], _h[s]) < 0) s = r;
                if (s == i) break;
                (_h[i], _h[s]) = (_h[s], _h[i]); i = s;
            }
            return min;
        }

        public ScheduleItem PeekMin() => _h[0];
        public IReadOnlyList<ScheduleItem> Snapshot() => _h.AsReadOnly();
        public void LoadFrom(IEnumerable<ScheduleItem> items) { _h.Clear(); foreach (var it in items) Push(it.Id, it.At); }
    }
}
```

- [ ] **Step 4: 运行通过** — `dotnet test --filter SchedulerTests` → PASS (1 passed).

- [ ] **Step 5: Commit**
```bash
git add src/Jianghu.Core/Sim/Scheduler.cs tests/Jianghu.Core.Tests/SchedulerTests.cs
git commit -m "feat: 确定性 (At,Id) 最小堆 Scheduler (R-NF3)"
```

---

### Task 14: World + Advance(budget) 事件循环 + 空间索引 (R-F2..F4 / R-NF3)

**Files:**
- Create: `src/Jianghu.Core/Sim/World.cs`, `src/Jianghu.Core/Sim/WorldFactory.cs`
- Test: `tests/Jianghu.Core.Tests/WorldAdvanceTests.cs`

- [ ] **Step 1: 写失败测试（无 freeze + 推进 + 编年史非空）**
```csharp
using Jianghu.Config;
using Jianghu.Sim;
using Xunit;

public class WorldAdvanceTests
{
    [Fact]
    public void Advance_respects_budget_and_progresses()
    {
        var w = WorldFactory.CreateInitial(seed: 1, LimitsConfig.Default, initialCount: 5);
        long c0 = w.Clock;
        w.Advance(budget: 3);                 // 至多处理 3 个决策
        Assert.True(w.Clock >= c0);
        Assert.True(w.Chronicle.Count > 0);   // 产出编年史
    }

    [Fact]
    public void Long_run_does_not_freeze_and_keeps_population_band()
    {
        var w = WorldFactory.CreateInitial(seed: 2, LimitsConfig.Default, initialCount: 5);
        for (int i = 0; i < 500; i++) w.Advance(budget: 8);
        Assert.InRange(w.AliveCount, 1, LimitsConfig.Default.PopulationHigh + 5);
        Assert.True(w.Chronicle.Count > 50);
    }
}
```

- [ ] **Step 2: 运行失败** — `dotnet test --filter WorldAdvanceTests` → 编译失败。

- [ ] **Step 3: 实现 World（实现 IWorldMutator）+ WorldFactory**

Create `src/Jianghu.Core/Sim/World.cs`:
```csharp
using System.Collections.Generic;
using System.Threading;
using Jianghu.Actions;
using Jianghu.Config;
using Jianghu.Decide;
using Jianghu.Events;
using Jianghu.Model;
using Jianghu.Random;
using Jianghu.Stats;

namespace Jianghu.Sim
{
    public sealed class World : IWorldMutator
    {
        public long Clock { get; private set; }
        public LimitsConfig Limits { get; }
        public Chronicle Chronicle { get; } = new Chronicle();
        public Relations Relations { get; } = new Relations();
        public List<WorldNode> Nodes { get; } = new List<WorldNode>();
        public Sect Sect { get; }
        public List<Character> Deceased { get; } = new List<Character>();

        private readonly Dictionary<long, Character> _alive = new Dictionary<long, Character>();
        private readonly Scheduler _sched = new Scheduler();
        private readonly ActionSystem _actions;
        private readonly Lifecycle _lifecycle;
        private readonly IRandom _domainRng;     // 决策外的领域随机（动作内随机用）
        public IRandom SpawnRng { get; }          // 独立 spawn 流
        private readonly Dictionary<CharacterId, IBrain> _brains = new Dictionary<CharacterId, IBrain>();

        public int AliveCount => _alive.Count;
        public int NodeCount => Nodes.Count;

        public World(LimitsConfig limits, IRandom domainRng, IRandom spawnRng, Sect sect, Lifecycle lifecycle)
        {
            Limits = limits; _domainRng = domainRng; SpawnRng = spawnRng; Sect = sect;
            _actions = new ActionSystem(limits); _lifecycle = lifecycle;
        }

        public void Add(Character c, IBrain brain)
        {
            _alive[c.Id.Value] = c; _brains[c.Id] = brain;
            c.NextActAt = Clock + _lifecycle.ActionInterval(c, Limits);
            _sched.Push(c.Id, c.NextActAt);
            Chronicle.Append(new CharacterBorn(Clock, c.Id), NameOf);
        }

        public string NameOf(CharacterId id)
        {
            if (_alive.TryGetValue(id.Value, out var c)) return c.Persona.Name;
            foreach (var d in Deceased) if (d.Id.Value == id.Value) return d.Persona.Name + "(故)";
            return "无名";
        }

        // —— IWorldView ——
        public IReadOnlyList<Character> AtNode(NodeId node)
        {
            var list = new List<Character>();
            foreach (var c in _alive.Values) if (c.Node.Value == node.Value) list.Add(c);
            list.Sort((a, b) => a.Id.Value.CompareTo(b.Id.Value)); // 确定性
            return list;
        }

        // —— IWorldMutator ——
        public void ApplyStat(Character c, StatKind k, int delta) => c.Stats.Apply(k, delta, Limits);
        public int AdjustRelation(CharacterId f, CharacterId t, int d) => Relations.Adjust(f, t, d);
        public void Move(Character c, NodeId to) => c.Node = to;

        /// <summary>事件驱动推进：处理至多 budget 个到期决策（§7.1）。</summary>
        public void Advance(int budget)
        {
            int processed = 0;
            while (!_sched.IsEmpty && processed < budget)
            {
                var item = _sched.PopMin();
                if (!_alive.TryGetValue(item.Id.Value, out var actor) || !actor.Alive) continue;
                Clock = item.At > Clock ? item.At : Clock;

                var ctx = BuildContext(actor);
                var choice = _brains[actor.Id].DecideAsync(ctx, CancellationToken.None).GetAwaiter().GetResult();
                var events = _actions.Execute(this, actor, choice);
                foreach (var e in events)
                {
                    Project(actor, e);
                    Chronicle.Append(e, NameOf);
                }
                _lifecycle.Tick(actor, this, out var died);
                if (died != null) { Chronicle.Append(died, NameOf); RemoveDead(actor); continue; }

                actor.NextActAt = Clock + _lifecycle.ActionInterval(actor, Limits);
                _sched.Push(actor.Id, actor.NextActAt);
                processed++;
            }
            _lifecycle.MaybeSpawn(this);
        }

        private void RemoveDead(Character c)
        {
            _alive.Remove(c.Id.Value);
            c.Alive = false; Deceased.Add(c);
        }

        public DecisionContext BuildContext(Character a)
        {
            var nearby = new List<NearbyActor>();
            foreach (var other in AtNode(a.Node))
            {
                if (other.Id.Value == a.Id.Value) continue;
                int power = other.Stats.Get(StatKind.Force) * 2 + other.Stats.Get(StatKind.Internal) + other.Stats.Get(StatKind.Constitution);
                nearby.Add(new NearbyActor(other.Id, power, Relations.Affinity(a.Id, other.Id)));
            }
            return new DecisionContext(a.Id, a.Stats, a.Goal, a.Node, nearby, _actions.Types, a.RecallMemory());
        }

        private void Project(Character actor, DomainEvent e)
        {
            switch (e)
            {
                case DuelResolved d:
                    actor.Remember(new MemoryEntry(d.Tick, "spar", d.Winner, d.Loser, d.Winner.Value == actor.Id.Value ? 2 : -2));
                    break;
                case CharacterTrained t:
                    actor.Remember(new MemoryEntry(t.Tick, "train", t.Id, null, 1));
                    break;
            }
            // Relations 已在动作内经 AdjustRelation 改；此处仅记忆投影
        }

        // —— 快照（v1.0 用深拷贝；JSON 序列化是 v1.1 §4.3）——
        public World Clone()
        {
            var lc = new Lifecycle(SpawnRng.Split(1));
            var clone = new World(Limits, CloneRng(_domainRng), CloneRng(SpawnRng), Sect, lc);
            clone.Clock = Clock;
            foreach (var n in Nodes) clone.Nodes.Add(n);
            foreach (var c in _alive.Values)
            {
                var cc = c.Clone();
                clone._alive[cc.Id.Value] = cc;
                clone._brains[cc.Id] = _brains[c.Id]; // RuleBrain 含内部 streak/last，需克隆见注
            }
            foreach (var it in _sched.Snapshot()) clone._sched.Push(it.Id, it.At);
            foreach (var d in Deceased) clone.Deceased.Add(d);
            return clone;
        }

        private static IRandom CloneRng(IRandom r)
        {
            var p = new Pcg32(0, 1); p.SetState(r.GetState()); return p;
        }
    }
}
```
> 注（克隆 RuleBrain 内部状态）：为让 R-NF2 续跑严格逐字节一致，`IBrain` 需可克隆。最小做法见 Task 16 Step 3（给 `RuleBrain` 加 `Clone()` 并在 `World.Clone` 调用）。本 Task 测试不依赖该严格性。

Create `src/Jianghu.Core/Sim/WorldFactory.cs`:
```csharp
using Jianghu.Config;
using Jianghu.Decide;
using Jianghu.Model;
using Jianghu.Random;
using Jianghu.Stats;

namespace Jianghu.Sim
{
    public static class WorldFactory
    {
        private static readonly string[] Xing = { "赵", "钱", "孙", "李", "周", "吴", "郑", "王", "燕", "独孤" };
        private static readonly string[] Ming = { "无忌", "寻欢", "飞", "玄机", "未央", "求败", "三", "九", "白", "青" };

        public static World CreateInitial(ulong seed, LimitsConfig limits, int initialCount)
        {
            limits.Validate();
            var root = new Pcg32(seed, 1);
            var genRng = root.Split(1);
            var domainRng = root.Split(2);
            var spawnRng = root.Split(3);
            var brainRngBase = root.Split(4);

            var sect = new Sect(1, "无名谷");
            var lifecycle = new Lifecycle(spawnRng.Split(99));
            var w = new World(limits, domainRng, spawnRng, sect, lifecycle);
            w.Nodes.Add(new WorldNode(new NodeId(0), "客栈"));
            w.Nodes.Add(new WorldNode(new NodeId(1), "山道"));
            w.Nodes.Add(new WorldNode(new NodeId(2), "市集"));

            for (int i = 0; i < initialCount; i++)
                w.Add(MakeCharacter(i, genRng, brainRngBase, limits), null == null ? null : null); // 见下方重载

            return w;
        }

        // 真正的角色 + Brain 构建（供 Factory 与 Lifecycle.MaybeSpawn 复用）
        public static (Character, IBrain) Spawn(long id, IRandom genRng, IRandom brainRngBase, LimitsConfig limits, long bornAt)
        {
            var arch = (ArchetypeKind)(genRng.NextInt(2));
            var goal = arch == ArchetypeKind.Martial ? GoalKind.Advance : GoalKind.Wander;
            var name = Xing[genRng.NextInt(Xing.Length)] + Ming[genRng.NextInt(Ming.Length)];
            var persona = new Persona(name, "江湖客", "市井", arch, 1);
            var stats = StatGenerator.Generate(genRng, limits);
            long lifespan = genRng.NextInclusive((int)limits.LifespanMin, (int)limits.LifespanMax);
            var ch = new Character(new CharacterId(id), persona, stats, new NodeId(genRng.NextInt(3)),
                new Goal(goal, 0), age: 0, lifespan: lifespan, memoryCap: 16);
            var brain = new RuleBrain(brainRngBase.Split((ulong)id), arch);
            return (ch, brain);
        }

        private static Character MakeCharacter(int i, IRandom genRng, IRandom brainRngBase, LimitsConfig limits) => null;
    }
}
```
> 注：上面 `CreateInitial` 的角色装配用 `Spawn(...)`。把 `for` 循环改为：
> ```csharp
> for (int i = 0; i < initialCount; i++)
> {
>     var (ch, brain) = Spawn(i, genRng, brainRngBase, limits, w.Clock);
>     w.Add(ch, brain);
> }
> ```
> 并删除占位的 `MakeCharacter`。（实现时直接写成此形，占位仅为说明装配点。）

- [ ] **Step 4: 运行通过** — 先实现 Task 15 的 `Lifecycle` 才能编译 World（World 依赖 Lifecycle）。因此**先做 Task 15，再回到本 Task Step 4**。两者循环依赖于编译，按"先 Lifecycle 骨架、再 World"的顺序：实际编码顺序 = Task15 → Task14。回到此处后 Run: `dotnet test --filter WorldAdvanceTests` → PASS (2 passed)。

- [ ] **Step 5: Commit**（与 Task 15 一起提交，见 Task 15 Step 5）

---

### Task 15: Lifecycle 衰老/死亡/新人涌现 (R-F5/F6 / §7.4)

> 实现顺序：本 Task 在 Task 14 之前编码（World 依赖它），但测试依赖 World，故测试随 Task 14 一起跑。

**Files:**
- Create: `src/Jianghu.Core/Sim/Lifecycle.cs`
- Test: `tests/Jianghu.Core.Tests/LifecycleTests.cs`

- [ ] **Step 1: 写失败测试**
```csharp
using Jianghu.Config;
using Jianghu.Sim;
using Xunit;

public class LifecycleTests
{
    [Fact]
    public void Population_replenishes_when_below_low_band()
    {
        var limits = LimitsConfig.Default with { PopulationLow = 5, LifespanMin = 30, LifespanMax = 40 };
        var w = WorldFactory.CreateInitial(seed: 3, limits, initialCount: 5);
        for (int i = 0; i < 300; i++) w.Advance(budget: 8); // 短寿 → 频繁死亡 → 必触发涌现
        Assert.True(w.Deceased.Count > 0, "应有角色寿尽");
        Assert.True(w.AliveCount >= 1, "应被涌现补充，不灭绝");
    }
}
```

- [ ] **Step 2: 运行失败** — `dotnet test --filter LifecycleTests` → 编译失败（Lifecycle 不存在）。

- [ ] **Step 3: 实现**

Create `src/Jianghu.Core/Sim/Lifecycle.cs`:
```csharp
using Jianghu.Config;
using Jianghu.Events;
using Jianghu.Model;
using Jianghu.Random;

namespace Jianghu.Sim
{
    /// <summary>衰老/死亡 + 人口低于下界时复用人设生成器涌现新人（spawn-RNG，§7.4）。</summary>
    public sealed class Lifecycle
    {
        private readonly IRandom _spawnRng;
        private long _nextId = 1_000_000; // 涌现者 Id 段，避免与初始 0..N 撞

        public Lifecycle(IRandom spawnRng) { _spawnRng = spawnRng; }

        public long ActionInterval(Character c, LimitsConfig limits)
        {
            // 错峰：基线 ± 抖动（用角色当前属性派生，确定性，不消耗共享 RNG）
            long jitter = (c.Stats.Get(Jianghu.Stats.StatKind.Insight) % 5);
            return limits.ActionIntervalBase + jitter;
        }

        public void Tick(Character actor, World world, out CharacterDied died)
        {
            actor.Age += ActionInterval(actor, world.Limits);
            if (actor.Age >= actor.Lifespan)
            {
                died = new CharacterDied(world.Clock, actor.Id, actor.Age);
                return;
            }
            died = null;
        }

        public void MaybeSpawn(World world)
        {
            while (world.AliveCount < world.Limits.PopulationLow)
            {
                long id = _nextId++;
                var genRng = _spawnRng.Split((ulong)id);
                var brainBase = _spawnRng.Split((ulong)id ^ 0xABCDEF);
                var (ch, brain) = WorldFactory.Spawn(id, genRng, brainBase, world.Limits, world.Clock);
                world.Add(ch, brain);
            }
        }
    }
}
```

- [ ] **Step 4: 运行通过** — 现在 World（Task 14）可编译。Run: `dotnet test --filter "LifecycleTests|WorldAdvanceTests"` → PASS (3 passed)。

- [ ] **Step 5: Commit（World + Lifecycle 一并）**
```bash
git add src/Jianghu.Core/Sim/World.cs src/Jianghu.Core/Sim/WorldFactory.cs src/Jianghu.Core/Sim/Lifecycle.cs tests/Jianghu.Core.Tests/WorldAdvanceTests.cs tests/Jianghu.Core.Tests/LifecycleTests.cs
git commit -m "feat: World 事件循环(预算防卡死) + Lifecycle 生老死+涌现 (R-F2..F6/R-NF3)"
```

---

### Task 16: 确定性 + 续跑复现 (R-NF1 / R-NF2)

**Files:**
- Modify: `src/Jianghu.Core/Decide/RuleBrain.cs`（加 `Clone()` + 复制 `_last/_streak`）, `src/Jianghu.Core/Sim/World.cs`（`Clone` 调用 brain.Clone）
- Test: `tests/Jianghu.Core.Tests/DeterminismTests.cs`

- [ ] **Step 1: 写失败测试**
```csharp
using System.Linq;
using Jianghu.Config;
using Jianghu.Sim;
using Xunit;

public class DeterminismTests
{
    private static string RunChronicle(ulong seed, int steps, int budget)
    {
        var w = WorldFactory.CreateInitial(seed, LimitsConfig.Default, 5);
        for (int i = 0; i < steps; i++) w.Advance(budget);
        return string.Join("\n", w.Chronicle.Lines);
    }

    [Fact]
    public void Same_seed_same_chronicle() // R-NF1
        => Assert.Equal(RunChronicle(2026, 200, 6), RunChronicle(2026, 200, 6));

    [Fact]
    public void Snapshot_continue_equals_uninterrupted() // R-NF2
    {
        var full = WorldFactory.CreateInitial(7, LimitsConfig.Default, 5);
        for (int i = 0; i < 120; i++) full.Advance(6);
        var fullText = string.Join("\n", full.Chronicle.Lines);

        var part = WorldFactory.CreateInitial(7, LimitsConfig.Default, 5);
        for (int i = 0; i < 60; i++) part.Advance(6);
        var clone = part.Clone();              // 快照
        for (int i = 0; i < 60; i++) clone.Advance(6);
        var cloneText = string.Join("\n", clone.Chronicle.Lines);

        Assert.Equal(fullText, cloneText);
    }
}
```

- [ ] **Step 2: 运行**

Run: `dotnet test --filter DeterminismTests`
Expected: `Same_seed_same_chronicle` PASS（全确定性路径）；`Snapshot_continue` 可能 FAIL——因 `World.Clone` 未克隆 RuleBrain 的 `_last/_streak`。下一步修。

- [ ] **Step 3: 给 RuleBrain 加 Clone，World.Clone 用之**

在 `src/Jianghu.Core/Decide/RuleBrain.cs` 增加（类内）：
```csharp
        public RuleBrain Clone()
        {
            var p = new Pcg32(0, 1); p.SetState(_rng.GetState());
            var b = new RuleBrain(p, _arch) { };
            b._last = _last; b._streak = _streak;
            return b;
        }
```
> 注：需在 `RuleBrain` 引入 `using Jianghu.Random;`（已有）。`_last/_streak` 为私有字段，Clone 在类内可访问。

在 `src/Jianghu.Core/Sim/World.cs` 的 `Clone()` 中，把 `clone._brains[cc.Id] = _brains[c.Id];` 改为：
```csharp
                clone._brains[cc.Id] = _brains[c.Id] is Jianghu.Decide.RuleBrain rb ? rb.Clone() : _brains[c.Id];
```

- [ ] **Step 4: 运行通过**

Run: `dotnet test --filter DeterminismTests`
Expected: PASS (2 passed)。同种子同编年史；快照续跑 == 不中断（逐字节）。

- [ ] **Step 5: Commit**
```bash
git add src/Jianghu.Core/Decide/RuleBrain.cs src/Jianghu.Core/Sim/World.cs tests/Jianghu.Core.Tests/DeterminismTests.cs
git commit -m "test: 同种子同输出 + 快照续跑逐字节复现 (R-NF1/R-NF2)"
```

---

### Task 17: Core 纯净回归测试 (R-NF4 / §4.2)

**Files:**
- Test: `tests/Jianghu.Core.Tests/CorePurityTests.cs`

- [ ] **Step 1: 写测试（断言 Core 程序集不引用桌面专属程序集）**
```csharp
using System;
using System.Linq;
using Xunit;

public class CorePurityTests
{
    [Fact]
    public void Core_does_not_reference_forbidden_assemblies()
    {
        var refs = typeof(Jianghu.Sim.World).Assembly.GetReferencedAssemblies().Select(a => a.Name);
        Assert.DoesNotContain("System.Net.Http", refs);
        Assert.DoesNotContain("System.Console", refs);
    }

    [Fact]
    public void Core_targets_netstandard()
    {
        var tfm = typeof(Jianghu.Sim.World).Assembly
            .GetCustomAttributes(typeof(System.Runtime.Versioning.TargetFrameworkAttribute), false);
        // netstandard2.1 程序集无 TargetFramework 运行时元数据要求；此处确保能从 net8 测试加载
        Assert.NotNull(typeof(Jianghu.Sim.World).Assembly.Location);
    }
}
```

- [ ] **Step 2: 运行** — `dotnet test --filter CorePurityTests` → 若 BannedApiAnalyzers 配置正确，Core 早已无禁用 API；此测试守住引用面。Expected PASS (2 passed)。

- [ ] **Step 3: Commit**
```bash
git add tests/Jianghu.Core.Tests/CorePurityTests.cs
git commit -m "test: Core 纯净回归(无禁用程序集引用) (R-NF4)"
```

---

### Task 18: CLI 宿主（驱动开放式模拟 + 打印编年史/人设卡）(R-F4/R-F7)

**Files:**
- Modify: `src/Jianghu.Cli/Program.cs`
- Test: 手动运行验收

- [ ] **Step 1: 实现 Program.cs**

Replace `src/Jianghu.Cli/Program.cs`:
```csharp
using System;
using Jianghu.Config;
using Jianghu.Sim;

ulong seed = args.Length > 0 && ulong.TryParse(args[0], out var s) ? s : 2026UL;
int steps = args.Length > 1 && int.TryParse(args[1], out var n) ? n : 200;
int budget = 8;

var world = WorldFactory.CreateInitial(seed, LimitsConfig.Default, initialCount: 8);

Console.WriteLine($"=== 江湖开演 (seed={seed}, steps={steps}) ===");
for (int i = 0; i < steps; i++) world.Advance(budget);  // 开放式：可改 while(true) 长跑

Console.WriteLine($"\n--- 江湖编年史（共 {world.Chronicle.Count} 条，节选末 25 条）---");
var lines = world.Chronicle.Lines;
for (int i = Math.Max(0, lines.Count - 25); i < lines.Count; i++) Console.WriteLine(lines[i]);

Console.WriteLine($"\n--- 当世人物（{world.AliveCount}）/ 逝者（{world.Deceased.Count}）---");
```

- [ ] **Step 2: 运行验收**

Run:
```bash
dotnet run --project src/Jianghu.Cli 2026 200
```
Expected: 打印开场、末 25 条武侠味编年史（修炼/切磋/行旅/寿尽/新人入江湖）、当世/逝者计数。两次同 `seed` 输出一致。

- [ ] **Step 3: Commit**
```bash
git add src/Jianghu.Cli/Program.cs
git commit -m "feat: CLI 宿主驱动开放式模拟 + 编年史输出 (R-F4/R-F7)"
```

---

### Task 19: 全量回归 + 文档收尾

- [ ] **Step 1: 全量测试**

Run: `dotnet test`
Expected: 全绿（覆盖 R-F1..F7、R-NF1..F4、R-NF6、R-NF7）。

- [ ] **Step 2: 在 spec 标注落地**

在 spec `§16 评审溯源` 后追加一行：`v1.0 实现见 docs/superpowers/plans/2026-06-13-江湖模拟内核-v1.0.md；R-ID 全覆盖。`（R-NF5 LLM 健壮性、R-F3 关系传播深化属 v1.1。）

- [ ] **Step 3: Commit**
```bash
git add -A
git commit -m "docs: v1.0 walking skeleton 完成, R-ID 覆盖标注"
```

---

## 自审（Self-Review）

**1. Spec 覆盖：**
- R-F1 人设生成 → Task 5/6/9/WorldFactory.Spawn ✓
- R-F2 枚举动作决策 → Task 10/11/12/14 ✓
- R-F3 DomainEvent 投影 → Task 8/14(Project) ✓
- R-F4 开放式连续推进 → Task 14(Advance)/18 ✓
- R-F5 死亡入逝者档 → Task 15 ✓
- R-F6 新人涌现 → Task 15(MaybeSpawn) ✓
- R-F7 编年史/人设卡 → Task 8/18 ✓
- R-NF1 同种子同输出 → Task 16 ✓
- R-NF2 快照续跑复现 → Task 16 ✓
- R-NF3 单步预算防卡死 → Task 13/14 ✓
- R-NF4 Core 纯净/netstandard2.1 → Task 0/17 ✓
- R-NF5 LLM 健壮 fallback → **v1.1**（v1.0 无 LLM，明确不覆盖）
- R-NF6 偏中庸分布 → Task 5 ✓
- R-NF7 属性上限不变量 → Task 4/5 ✓
- §7.5 空间索引：v1.0 用 `AtNode` 线性扫描（5-30 角色足够）；O(节点占用) 的桶索引在角色增多时再加（标注 backlog，不阻塞 v1.0）。

**2. 占位符扫描：** 无 TBD/TODO。Task 14 的 `MakeCharacter` 占位已在注中明确替换为 `Spawn` 装配（实现时直接写最终形）。

**3. 类型一致性：** `IRandom`(NextUInt/NextInt/NextInclusive/GetState/SetState/Split)、`StatBlock.Apply(StatKind,int,LimitsConfig)`、`IWorldMutator`(ApplyStat/AdjustRelation/Move)、`IBrain.DecideAsync→ValueTask<ActionChoice>`、`ActionChoice`(TrainChoice/TravelChoice/SparChoice)、`Character.Clone`/`RuleBrain.Clone`/`World.Clone` 跨任务签名一致。`WorldFactory.Spawn` 同时被 Factory 与 `Lifecycle.MaybeSpawn` 复用，签名统一。

**4. 二义性：** Task 14↔15 编译循环依赖已显式定序（先 Lifecycle 实现、World 测试与 Lifecycle 合并跑）。`SetState` 仅恢复 `_state`（`_inc` 构造固定），已在 Task 1 测试覆盖往返。

---

## 执行交接

**Plan complete and saved to `docs/superpowers/plans/2026-06-13-江湖模拟内核-v1.0.md`. Two execution options:**

**1. Subagent-Driven (recommended)** — 每个 Task 派新 subagent、任务间评审、快迭代。

**2. Inline Execution** — 本会话内按 executing-plans 批量执行 + 检查点。

**Which approach?**
