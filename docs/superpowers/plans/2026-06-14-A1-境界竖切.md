# A.1 境界竖切（统一大小境界 + 三轴投影 + UT 迁移）Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development（推荐）执行，task-by-task。Steps 用 checkbox。**所有 implementer/reviewer subagent 用 Opus 4.8。**

**Goal:** 给 A.0 的 21 路修炼系统落「统一大小境界双层 + 三轴解耦投影 + UT 平衡迁移」的**地基**：RealmCurveDef 加 SubLevelCount/CanAscend/MaxMajor 三列；前缀和投影 flatIndex↔(大境界,小境界)；统一双轨命名替换；4 偏离路 UT 迁移 + 辅助路战斗当量重锚。深度层（流程/劫/寿元/道心/破单调）= 后续 plan。

**Architecture:** A.0 `RealmCurveDef`（四列等长）扩为多列；新 `RealmProjection`（前缀和 encode/decode，纯整数确定）；`RealmCurve.Validate` 扩多列校验；21 路 paths/*.cs 构造器迁移 + UnifiedTierOf 修正（4 偏离路）；RealmNames 统一替换（纯 flavor，不破 determinism）。

**Tech Stack:** C#/.NET（Jianghu.Core netstandard2.1 禁浮点；xUnit）。master 分支，A.0 已建 204 测试绿。

**权威来源：** `docs/superpowers/specs/2026-06-14-v1.2-A1-统一大小境界与平衡对应-design.md`（境界基线 + §11 审计修正）；`docs/superpowers/specs/2026-06-14-v1.2-A123-收敛对齐设计.md` §0/§A1。

**A.0-as-built 基线（实读，auditor 核过属实）：** `RealmCurveDef`（CultivationSchema.cs）现 4 列；`RealmCurve.Validate`（RealmCurve.cs:26）只校验四列等长；`PathValidator.AssertValid`（PathRegistry.cs:106）调 RealmCurve.Validate；21 路 paths/*.cs 用 4 参 RealmCurveDef ctor；21 standalone 测试断言 `RealmMultipliers.Count==UnifiedTierOf.Count`；`RealmCurveTests.cs:8 Curve3()`/`:30 bad` 用 4 参 ctor（**加列必改这两个 fixture**）；Chronicle 用整数 NewRealmIndex 渲染（不读名）；CLI Program.cs 才查 RealmNames。

**全局红线：** 禁浮点；前缀和投影纯整数确定；**off（cultivation=false）逐字节守**（加列/改名不应改运行期行为——A.0 的 PowerEngine/NextIndexIfReady 不读新列，故行为不变，38+ off 测试逐字节）；新列数据与 flatIndex 一致（Σ SubLevelCount == flatIndex 数）。

**分支：** 新建 `feat/jianghu-v1.2-A1`（off master）。

---

## 分支 4 偏离路（auditor B1 实测，迁移前置数据）

| 路 | A.0 现 UnifiedTierOf | 迁移目标 |
|---|---|---|
| 体 ti_xiu_hengshi | `0,1,2,4,6,8,9,10,11,12` | 删 UT1 → 该 flatIndex 并入相邻大境界做小境界；UnifiedTierOf 走偶数主阶 |
| 命 ming_fate_causality | `0,1,3,5,7,8,9,10,11,12` | UT1/3/5/7 压偶数主阶 0/2/4/6/8（密度走 SubLevelCount 加密），顶段 8/9/10/11/12 不变 |
| 符 fu_xiu_fulu | `0,1,2,3,4,6,8,10` | UT1/3 并入小境界；**顶 UT10→补 UT11/12**（渡劫/飞升，符是修士） |
| 佛 buddhist_golden_body | `…,9,10,11,12,12`(dup) | plateau：顶大境界 SubLevelCount=2（金身/不坏金身共 UT12） |

---

## Task A1.0a: RealmCurveDef 加三列（schema，先不改 Validate 逻辑）

**Files:**
- Modify: `src/Jianghu.Core/Cultivation/CultivationSchema.cs`（RealmCurveDef record）
- Test: `tests/Jianghu.Core.Tests/Cultivation/RealmCurveTests.cs`

- [ ] **Step 1: 写失败测试**（构造含新列的 RealmCurveDef）
```csharp
[Fact] public void RealmCurveDef_HasSubLevelCount_CanAscend_MaxMajor() {
    var c = new RealmCurveDef(
        new[]{10,15,25}, new[]{0,2,4}, new[]{"炼气","筑基","金丹"}, new[]{0,100,300},
        subLevelCount: new[]{1,1,1}, canAscend: true, maxMajor: 2);
    Assert.Equal(3, c.SubLevelCount.Count);
    Assert.True(c.CanAscend);
    Assert.Equal(2, c.MaxMajor);
}
```
- [ ] **Step 2: 跑确认失败**（RealmCurveDef ctor 无新参）。
- [ ] **Step 3: 实现**：RealmCurveDef 加 `IReadOnlyList<int> SubLevelCount, bool CanAscend, int MaxMajor`（spec/境界稿 §2）。**这会编译断 21 路 ctor + Curve3()/bad fixture**——本 task 仅改 record + 让其编译，21 路迁移在 A1.0b（先让 record 定义就位，21 路暂用占位新参由 A1.0b 填真值）。为先过编译，可给新参默认值 OR 本 task 连带改 21 路（见 A1.0b，二者可合并一次提交）。
- [ ] **Step 4: 跑确认通过**。
- [ ] **Step 5: 提交**。

## Task A1.0b: 21 路构造器 + fixtures 迁移（填三列真值，off 逐字节）

**Files:**
- Modify: 全 21 `src/Jianghu.Core/Cultivation/paths/*.cs`；`tests/.../Cultivation/RealmCurveTests.cs`（Curve3/bad）；21 standalone 测试（补新列断言）
- Test: 全量回归

- [ ] **Step 1: 写失败测试**（21 路 RealmCurveDef 含一致新列：SubLevelCount.Count==大境界数、Σ SubLevelCount==RealmMultipliers.Count、CanAscend=true、MaxMajor=大境界数-1）
```csharp
[Fact] public void AllPaths_HaveConsistentSubLevelData() {
    foreach (var p in new CodePathSource().Load()) {
        var c = p.Curve;
        Assert.Equal(c.RealmMultipliers.Count, c.SubLevelCount.Sum()); // 前缀和闭合
        Assert.True(c.SubLevelCount.All(s => s >= 1));
        Assert.True(c.CanAscend);          // 21 修真路全 true（武夫 defer）
        Assert.Equal(c.SubLevelCount.Count - 1, c.MaxMajor);
    }
}
```
- [ ] **Step 2: 跑确认失败**。
- [ ] **Step 3: 实现**：逐路给 RealmCurveDef 补 SubLevelCount/CanAscend=true/MaxMajor。**SubLevelCount 推导**：A.0 现每路 flatIndex 数（RealmMultipliers.Count）= 大境界数 × 小境界数的展开；A.1 以「大境界数 = UnifiedTierOf 的去重 UT 数（或按境界稿主阶梯）」反推每大境界 SubLevelCount（多数路 flatIndex≈大境界数 → SubLevelCount 多为 1；密集路如命修小境界多）。**本 task 暂以「每大境界 SubLevelCount=1」起步**（即 flatIndex==大境界，最简），4 偏离路的 SubLevel 加密在 A1.2 处理。CanAscend=true、MaxMajor=RealmMultipliers.Count-1。改 Curve3()/bad fixture 为新参。
- [ ] **Step 4: 跑确认通过 + 全量 204 测试绿（off 逐字节：新列不进运行期，行为不变）**。
- [ ] **Step 5: 提交**。

## Task A1.1: RealmCurve.Validate 扩多列校验

**Files:** Modify `src/Jianghu.Core/Cultivation/RealmCurve.cs`；Test `RealmCurveTests.cs`
- [ ] **Step 1: 写失败测试**（Σ SubLevelCount≠flatIndex 数 抛；SubLevelCount 含 0 抛；CanAscend=false 但 Last()>9 抛；UnifiedTierOf 降序 抛）
```csharp
[Fact] public void Validate_RejectsSubLevelSumMismatch() {
    var bad = Curve(mult:new[]{10,15,25}, ut:new[]{0,2,4}, sub:new[]{1,1}); // Σ=2≠3
    Assert.Throws<InvalidOperationException>(() => RealmCurve.Validate(bad));
}
[Fact] public void Validate_RejectsWuxiaTopAboveUT9() {
    var bad = Curve(ut:new[]{0,2,12}, canAscend:false); // 武夫顶 12>9
    Assert.Throws<InvalidOperationException>(() => RealmCurve.Validate(bad));
}
[Fact] public void Validate_RejectsNonMonotonicUT() {
    var bad = Curve(ut:new[]{0,4,2}); Assert.Throws<InvalidOperationException>(() => RealmCurve.Validate(bad));
}
```
- [ ] **Step 2: 跑确认失败**。
- [ ] **Step 3: 实现**：Validate 扩——四列等长（沿用）+ `Σ SubLevelCount == RealmMultipliers.Count` + `SubLevelCount.Count == MaxMajor+1` + `每 SubLevelCount[i]≥1` + `UnifiedTierOf 非降` + `!CanAscend ⇒ UnifiedTierOf.Last()≤9`（境界稿 §2/§11.2）。
- [ ] **Step 4: 跑确认通过 + 21 路全过 Validate（A1.0b 数据须合规）**。
- [ ] **Step 5: 提交**。

## Task A1.2: RealmProjection 前缀和投影 + INV-REALM-1 + 4 偏离路迁移

**Files:** Create `src/Jianghu.Core/Cultivation/RealmProjection.cs`；Modify 4 偏离路 paths；Test `RealmProjectionTests.cs`
- [ ] **Step 1: 写失败测试**（encode/decode 可逆 INV-REALM-1；佛修 plateau dup-UT 用例；4 偏离路迁移后 UnifiedTierOf 落锚集）
```csharp
[Fact] public void Projection_EncodeDecodeRoundTrips() { // INV-REALM-1
    var sub = new[]{1,1,1,1,1,1,1,1,2}; // 佛修 plateau：major8 含 2 小境界
    for (int fi=0; fi<sub.Sum(); fi++) {
        var (m,s) = RealmProjection.Decode(fi, sub);
        Assert.Equal(fi, RealmProjection.Encode(m, s, sub));
    }
}
[Fact] public void DeviantPaths_MigratedToAnchorSet() {
    var anchor = new HashSet<int>{0,2,4,6,8,9,10,11,12};
    foreach (var id in new[]{"ti_xiu_hengshi","ming_fate_causality","fu_xiu_fulu","buddhist_golden_body"})
        foreach (var ut in PathById(id).Curve.UnifiedTierOf) Assert.Contains(ut, anchor);
}
```
- [ ] **Step 2: 跑确认失败**。
- [ ] **Step 3: 实现**：`RealmProjection.Encode(major,sub,subLevelCount)`（offsets 前缀和 + offsets[major]+sub）；`Decode(flatIndex,subLevelCount)`（最大 m 使 offsets[m]≤fi）。**迁移 4 偏离路 UnifiedTierOf**（按分支表）：体删 UT1（并小境界）；命压 UT1/3/5/7→偶数主阶（对应大境界 SubLevelCount 加密）；符 UT1/3 并小境界 + 顶补 UT11/12（加 2 大境界 realmMultipliers/names/thresholds）；佛 plateau（顶 SubLevelCount=2）。改后各路 Validate 仍过 + standalone count 断言同步。
- [ ] **Step 4: 跑确认通过 + 全量绿（off 逐字节：UnifiedTierOf 改的是新列消费方向？——UnifiedTierOf 在 A.0 仅 CLI/测试读，运行期 PowerEngine 用 RealmMultipliers[RealmIndex] 不读 UT，故 off+on 战斗逐字节不变；符加 2 大境界改 RealmMultipliers 长度 → 影响符修 on 轨迹，但 off 无符修，且 on determinism 同种子仍自洽——跑 INV-DET-Cult 确认）**。
- [ ] **Step 5: 提交**。

## Task A1.3: 统一双轨命名替换（RealmNames，改名不破 determinism）

**Files:** Modify 21 路 paths（RealmNames）；Test `RealmNamingTests.cs`
- [ ] **Step 1: 写失败测试**（21 路 RealmNames 来自统一方案：主流修真=仙侠轨；体/佛=金身变体；鬼/魔/妖=邪道变体；儒=文位；名与 (大境界,小境界) 投影一致）
```csharp
[Fact] public void RealmNames_FollowUnifiedTracks() {
    // 主流路含「炼气/筑基/金丹/元婴/化神」；体修含「淬体/易筋/金身」；鬼修含「聚煞/养鬼/鬼王」；儒含「童生/秀才/进士」
    Assert.Contains("金丹", PathById("fa_xiu").Curve.RealmNames);
    Assert.Contains("不坏金身", PathById("buddhist_golden_body").Curve.RealmNames);
    Assert.Contains("童生", PathById("ru_xiu_haoran").Curve.RealmNames);
}
[Fact] public void Rename_DoesNotBreakDeterminism() { // off 逐字节 + on 同种子（Chronicle 走整数不读名）
    var a = WorldFactory.CreateInitial(seed:7); var b = WorldFactory.CreateInitial(seed:7);
    a.Advance(300); b.Advance(300); Assert.Equal(a.Chronicle.Render(), b.Chronicle.Render());
}
```
- [ ] **Step 2: 跑确认失败**。
- [ ] **Step 3: 实现**：21 路 RealmNames 替换为境界稿 §1/§4 统一双轨 + 变体名（按 (大境界,小境界) 投影：每 flatIndex = 大境界名 + 小境界后缀如「初期/中期/后期/圆满」）。**论证改名不破 determinism**：Chronicle/StateSnapshot 走整数 RealmIndex（境界稿 §7.1），仅 CLI 查名 → off 逐字节 + on 同种子不变。
- [ ] **Step 4: 跑确认通过 + 全量绿 + off 逐字节**。
- [ ] **Step 5: 提交**。

## Task A1.4: 辅助路 UT 重锚战斗当量（只动 UnifiedTierOf 不动 mul）

**Files:** Modify 辅助路 paths（丹/器/阵/符/驭兽/音/毒蛊 的 UnifiedTierOf）；Test `AuxiliaryUTAnchorTests.cs`
- [ ] **Step 1: 写失败测试**（辅助路 UnifiedTierOf 顶值 = 战斗当量非 UT12；RealmMultipliers 严格递增不破——M1）
```csharp
[Fact] public void AuxiliaryPaths_AnchorCombatUT_NotSeniority() {
    // 丹修战斗弱 → 顶 UT ≤ 8（不再 UT12）；地位走 path-local（itemTier/recipeCount）
    Assert.True(PathById("dan_xiu").Curve.UnifiedTierOf.Max() <= 8);
}
[Fact] public void AuxiliaryRemap_KeepsMultipliersNonBroken() { // M1：只动 UT 不动 mul
    foreach (var id in AuxiliaryPaths) {
        var m = PathById(id).Curve.RealmMultipliers;
        for (int i=1;i<m.Count;i++) Assert.True(m[i] >= m[i-1]); // 非降不破（yu_shou 有严格递增硬测试）
    }
}
```
- [ ] **Step 2: 跑确认失败**。
- [ ] **Step 3: 实现**：辅助路（丹/器/阵/符/驭兽/音/毒蛊）UnifiedTierOf 顶降到战斗当量（境界稿 §4/§5）——**只改 UnifiedTierOf 列，RealmMultipliers/Names/Thresholds 不动**（M1）。各路战斗当量 UT 顶由设计估（丹≈UT8/符≈UT8/阵≈UT9…，精确值标定期 INV-CROSS 收敛，本 task 给合理初值）。
- [ ] **Step 4: 跑确认通过 + 全量绿（standalone 仅 count 断言不破；yu_shou mul 严格递增不动）**。
- [ ] **Step 5: 提交**。

## Task A1.5: 三轴查询 API + Chronicle 大小境界渲染升级

**Files:** Create `RealmView.cs`（投影查询）；Modify `Events/Chronicle.cs`（RealmBreakthrough 渲染升级，可选）；Test
- [ ] **Step 1: 写失败测试**（给 flatIndex 查 (大境界名,小境界名,UT)；RealmBreakthrough 渲染含大小境界——但**不破 off 逐字节**：off 无修士无此事件）
- [ ] **Step 2/3: 实现**：`RealmView.Of(curve, flatIndex)` → (大境界名, 小境界名, UT)（前缀和投影 + RealmNames）。Chronicle 的 RealmBreakthrough 渲染可升级为「某入金丹·中期」（A.0 现渲染整数 index；升级仅改 on 路径新事件文案，off 无此事件逐字节）。
- [ ] **Step 4/5: 跑通过（含 off 逐字节）+ 提交**。

## Task A1.6: 境界竖切 gate（交 auditor）
- [ ] 全量 dotnet test 绿 + 0 警告 + IL 浮点零。
- [ ] INV-REALM-1（投影可逆，含佛 plateau）/ INV-UT-MONO（21 路 UnifiedTierOf 非降 + 落锚集）/ off 逐字节（worktree sha256 对比 A.1 前后 cultivation-off CLI）/ INV-DET-Cult（on 同种子 + Clone 续跑，含符修加境界后）全绿。
- [ ] dispatch auditor 审：4 偏离路迁移正确、辅助路只动 UT、改名不破 determinism、新列 Validate 真 gate、武夫 defer 诚实。

## Self-Review（plan 对设计核对）
- 覆盖境界稿 §2（schema 三列→A1.0a/b/A1.1）/§6 投影（A1.2 INV-REALM-1）/§11.1 4 偏离路（A1.2）/§11.3 佛 plateau（A1.2）/§11.4 辅助路只动 UT（A1.4）/§4 命名（A1.3）/§3 武夫 defer（A1.0b CanAscend=true 全填 + Validate 校验 false 分支）。
- 红线：off 逐字节（每 task Step 4 验）/ 禁浮点 / 投影纯整数 / 新列不进运行期（行为不变）。
- 深度层（流程/劫/寿元/道心/破单调/奇遇/转职/标定）= 后续 plan，非本竖切。
