# 战斗系统（Combat System）

> **Status**: In Design
> **Author**: huangjiaqi13 + Claude（/design-system 务实路径，前置 game-concept/systems-index 待 P8 补）
> **Last Updated**: 2026-06-16
> **Implements Pillar**: 武侠/仙侠"以快打慢·见招拆招·门派各异"的战斗质感
> **真相源（上游）**: `docs/superpowers/specs/2026-06-14-v1.2-B5-R2战斗系统重设计-design.md`（R2 回合循环 §4 + 功法门控 §14.1）、`...模块化效果系统-design.md`（EffectOp 模块/Special）、`...平衡标定INV-CROSS-design.md`（pe 量级锚）

---

## Overview

战斗系统决定两个角色（或多个 NPC）如何分出胜负。它**分两层**：

- **结算层（Core，纯整数确定，headless 可跑）**：速度（speed）决定行动序列（CTB 风），每个行动单位轮到时选一个**操作**（攻/防/控/资源/签名），经 `ModuleResolver` + `SpecialModuleRegistry` 把该操作的 `EffectOp` 模块作用到战斗状态。NPC×NPC 的江湖涌现战斗全在这一层，确定可复现（红线 B.2）。
- **即时层（Unity 宿主，后期）**：少数**瞬时反应操作**（闪避/格挡/打断/反弹/夺舍续命/逆演）有一个**时间窗口**；玩家介入时用**按键**在窗口内反应（33 号远征队式），NPC 则由 AI 按"命中窗口的概率/精度"模拟。**两种输入源共用同一套窗口数学**——这是涌现模拟与即时手感不冲突的关键。

核心区分（用户定）：**不是所有防御都即时**。常规防御（运气防御/护体真气/招架值/结界）是**回合内的选择**，在 Core 正常结算；只有"瞬时反应类"（闪/格/打断/反弹/续命/回滚）才有即时窗口。

战斗能做什么由**修了什么功法**决定（功法门控，§14.1）——stat 只调强弱，不自带能力（没轻功不能闪、没内功心法没护体真气）。

---

## Player Fantasy

- **当前阶段（v1 涌现模拟）**：玩家是"江湖的观察者/编剧"，看 NPC 们按各自功法路数自动厮杀，涌现出"剑仙一击破百、毒修阴蛊缠身、体修硬抗反震、命修逆天改命"的武侠桥段。战斗的爽感来自**门派各异的质感**和**涌现的戏剧性**（A 压制 B 三招，C 濒死夺舍翻盘）。
- **后期阶段（Unity 玩家介入）**：玩家操控一个角色时，常规出招是回合策略（选招、调度资源），但**关键时刻的闪/格/打断是即时按键**——在敌方杀招落下的一瞬精准格挡（铁山靠反震）、看准蓄力读条点穴打断、濒死时电光石火间夺舍续命。**"该出手时凭反应，该谋算时凭策略"**——回合谋略 + 即时反应的复合快感，正是 33 号远征队的精髓移植到武侠。

参考：宝可梦/最终幻想（速度序列）、八方旅人 CTB、33 号远征队（回合骨架 + 即时 parry/dodge 窗口）、北大侠客行（通用武学维 + 门派武功）。

---

## Detailed Design

### 核心循环（速度序列 + 操作结算）

```
战斗开始：每方 HP = pe（PowerEngine.Evaluate，已校准；同 UT 同量级 = 平衡锚）
          每个单位算 speed（见公式）→ 入行动队列（CTB：行动条/ATB 累积）

行动循环（直到一方 HP≤0 或达回合上限 N）：
  1. 取队列中 行动条最先满 的单位 A（speed 高者更频繁行动）
  2. A 选一个【操作】（NPC 由 AI 决策；玩家由输入）：进攻/防御/控制/资源/签名
  3. 结算该操作的 EffectOp 模块（经 ModuleResolver / SpecialModuleRegistry）
     - 若是【攻击】且目标有【即时反应窗口操作】待命 → 触发窗口判定（见下）
  4. 应用结果（伤害/控制/资源移动/状态），状态进 [Min,Cap] 钳
  5. A 的行动条清空，按 speed 重新累积
胜者 = HP 高者；平 → Tiebreak(CharacterId)
```

> **与 R2 原稿 §4 的差异**：原稿是"3 回合双方同时结算无先手"。本设计**升级为速度序列**（承用户构想）——speed 决定行动频率（快者多动），引入先手/多发。同时结算的"无先手偏置"由**速度模型本身的对称性 + 平衡标定**保证 C1 胜率契约（见 Edge Cases）。

### 操作分类学（taxonomy —— 本 GDD 核心，用户要求"每种操作属性分类"）

每个战斗操作按 **bucket（类型）× 时性（即时/回合选择）× 是否需时间窗口** 三轴分类。基于 21 路全战技归纳：

| Bucket | 操作 | 时性 | 即时窗口 | 代表招（路） |
|---|---|---|---|---|
| **进攻** | FlatPen 定值穿透 | 回合choice | 无 | 破·御剑诀（剑） |
| 进攻 | PenFromResource 资源转伤 | 回合choice | 无 | 剑二十三（剑）/燃血狂屠（血） |
| 进攻 | AoePerTarget 群攻 | 回合choice | 无 | 万剑归宗（剑） |
| 进攻 | CounterMul 克制倍乘 | 回合choice | 无 | 普化天雷·诛邪（雷，evil×3） |
| 进攻 | 自爆（清空资源） | 回合choice | 无 | 血河倾泻（魔） |
| 进攻 | pierce 破防 | 回合choice | 无 | 遇强破式（剑） |
| 进攻 | summon-attack 召唤齐攻 | 回合choice | 无（Σ derived→FULLSTRUCT） | 万鬼夜行（鬼） |
| 进攻 | execute 斩杀终结 | 回合choice | 条件判定 | 一指定数·绝命（因果） |
| **防御** | AddFlatDR 护体真气减伤 | 回合choice | 无 | 金钟落锁（体） |
| 防御 | 结界/律场护体 | 回合choice | 无（预布置） | 金刚伏魔圈（佛） |
| 防御 | 硬抗/不灭金身 | **即时** | 低（被动触发） | 不灭金身（体） |
| 防御 | **闪避 dodge** | **即时** | ★★★★ | 剑遁·三十六天罡（剑） |
| 防御 | **格挡/招架 parry** | **即时** | ★★★★★ | 横练护体·铁山靠（体） |
| 防御 | **反弹 reflect** | **即时** | ★★★★ | 玄黄护宝罡（器） |
| **控制** | 困/定身 stun | **即时** | ★★★★ | 勾魂索命（鬼）/须弥困界（因果） |
| 控制 | **打断蓄招 interrupt** | **即时** | ★★★★ | 金身震（体）/五雷轰顶（雷） |
| 控制 | 封印/沉默 | 回合choice | 无 | 五行封灵阵（法） |
| 控制 | 减速/拉距 | 回合choice | 无 | 乱石迷踪阵（阵） |
| 控制 | 心智控制 mind_control | 回合choice | 无 | 引商刻羽·乱心调（音） |
| 控制 | 命运先手压制 | **即时** | ★★★ | 一念抹因（因果） |
| **资源** | drain 夺取 | 回合choice | 无 | 夺运截命·一击（命） |
| 资源 | 燃心/暴走（高风险增益） | 回合choice | 无 | 燃心狂魔（魔） |
| 资源 | 蓄力充能 | 回合choice | 无 | 祭剑·见血（剑） |
| 资源 | 回复/疗愈 | 回合choice | 无 | 噬血反哺（血） |
| 资源 | 团队 BUFF | 回合choice | 无 | 十面埋伏·战阵乐（音） |
| 资源 | DOT 持续伤 | 回合choice | 无（挂载后自动） | 瘟疫毒雾（毒蛊） |
| **签名** | 落宝夺器 | **即时** | ★★★★ | 落宝金光（器） |
| 签名 | 夺舍续命 | **即时** | ★★★★★ | 夺舍·借尸还魂（鬼/魔/魂） |
| 签名 | 逆演栈回滚 | **即时** | ★★★★★ | 逆演重开（命/因果） |
| 签名 | 因果改定数 | 回合choice | 无 | 夺定数·截命一击（因果） |
| 签名 | 律场激活 | 回合choice | 准备窗口 | 起调·点律（音） |
| 签名 | 布阵点枢 | 回合choice | 准备窗口 | 落阵·点枢（阵） |

> **分类原则**：**即时窗口 = "在对手某个动作的极短时段内反应，时机决定成败"**。攻击/蓄力/挂 DOT/上 BUFF 这类"自己主动做、何时做无所谓"的，都是回合 choice。闪/格/打断/反弹/续命/回滚这类"对外部事件的瞬时反应"，才有窗口。

### 即时窗口模型（用户定，核心机制）

当攻击方发动一次攻击、防御方持有对应的**即时反应操作**（闪/格/打断/反弹/续命）时，开一个**时间窗口**：

- **窗口 = 一段时长**，中心是"最佳应对点"。
- **正态分布判定**：应对时刻偏离窗口中心越近，效果越好；偏离越远，效果衰减；窗口外基本无效。判定值 `q = exp(-(Δ/σ)²/2)`（Δ=偏离中心、σ=窗口宽度的 1/3，纯整数实现用查表/定点近似）。
- **成功（q 高）→ 引发该功法对应的完整效果**：点穴→打断对方蓄招、截拳→打断+反击、轻功闪避→免伤+换位、铁山靠→入伤×½反震、夺舍→濒死续命…
- **窗口宽度由两要素决定**：① **操作类型**（不同行为窗口不同：格挡窗口窄但回报高、闪避窗口中等、打断要踩对方蓄力读条）② **功法品级**（品阶越高窗口越宽/容错越高——同样是闪，凡阶轻功窗口窄，仙阶踏雪无痕窗口宽）。

**两种输入源（同一窗口数学）**：
- **玩家（Unity）**：实际按键时刻 vs 窗口中心 → 算 Δ → q（真·即时手感）。
- **NPC（Core）**：无人按键 → AI 用 `q_npc = f(功法品级, 角色 speed/悟性, 难度)` 直接采样一个"命中精度"（确定性：同种子同结果，红线 B.2）。**NPC 不需要真实时帧**，窗口退化为一个概率/精度数值。

> **这是分层的接缝**：窗口模型的**数学（窗口宽度表 + 正态判定 + 功法效果触发）放 Core**（NPC 用、确定）；窗口的**实时呈现 + 按键采集放 Unity 宿主**（玩家用）。Core 不引入实时帧循环——红线 B.2 守。

### 功法门控防御（承 R2 §14.1）

战斗能做什么 = 修了什么功法定。防御分模式：
- **硬抗**（人人，floor）= 体质削减伤害。
- **护体真气**（需修内功心法）→ 减伤（回合 choice）。
- **闪避**（需修轻功/身法功法）→ 即时窗口（没轻功不能闪）。
- **格挡/反弹**（需修横练/护体类）→ 即时窗口。
- **打断**（需修特定功法：点穴/截拳/雷法）→ 即时窗口。
公共武技池（神行/吐纳护体/基础招）= 散修兜底，靠 pick 得基础能力。

### 操作与模块系统的关系

每个操作 = 一组 `EffectOp` 模块（已实现，batch2/3）。即时窗口判定结果 `q` 作为**乘子**作用到该操作的模块效果上（q=1 满效果，q 低衰减）。NPC 的 q 由 AI 采样，玩家的 q 由按键。**模块系统不变，只在"即时类操作"的结算前插一个 q 乘子**。

---

## Formulas

### speed（行动频率）

`speed = base + Insight×Ks + 身法功法bonus − 负重penalty`

| 变量 | 类型 | 范围 | 说明 |
|---|---|---|---|
| base | int | 100 | 基础行动速度常数 |
| Insight | int | 1–40 | 悟性（身法/先手主属性，§2 战斗维映射） |
| Ks | int | 2 | 悟性→speed 权重（调优旋钮） |
| 身法功法bonus | int | 0–60 | 修轻功/身法功法的加成（功法门控） |
| 负重penalty | int | 0–40 | 重甲/重兵器减速 |

**行动条**：每"tick"所有单位 `行动条 += speed`；先到 `ACTION_THRESHOLD`(=1000) 者行动，行动后清零。speed 高 → 行动条满得快 → 行动更频繁（CTB 标准）。**纯整数确定**。

### 即时窗口判定 q

`q = round( Q_MAX × exp( −(Δ² ) / (2σ²) ) )`，整数实现用预计算正态查表（256 档）。

| 变量 | 类型 | 范围 | 说明 |
|---|---|---|---|
| Δ | int | 0–window | 应对时刻偏离窗口中心（玩家=按键偏差；NPC=AI 采样偏差） |
| σ | int | window/3 | 标准差 = 窗口宽度/3（正态 3σ 覆盖全窗） |
| window | int | 见下表 | 窗口总宽度（由操作类型 + 功法品级定） |
| Q_MAX | int | 100 | 满效果值（q∈[0,100]，作效果乘子 /100） |

**window 宽度表**（操作类型基值 × 功法品级系数）：

| 操作 | 基值 window₀ | 品级系数 |
|---|---|---|
| 格挡 parry | 30（窄，高回报） | ×(100+tier×15)/100 |
| 闪避 dodge | 50（中） | ×(100+tier×12)/100 |
| 打断 interrupt | 40（要踩蓄力） | ×(100+tier×15)/100 |
| 反弹 reflect | 35 | ×(100+tier×12)/100 |
| 夺舍续命 | 25（极窄，生死一线） | ×(100+tier×20)/100 |
| 逆演回滚 | 30 | ×(100+tier×18)/100 |

> tier=功法品级（1–5，ArtDef.Tier）。品阶越高窗口越宽 → 容错越高 → 高功法的反应更稳。

### NPC q 采样（确定性）

`q_npc = clamp( base_skill + 品级bonus + 悟性bonus − 难度penalty + 确定性扰动(seed), 0, 100 )`
- 同种子同输出（红线 B.2）；扰动来自 Pcg32 流，不破复现。

### 伤害（承 R2 §4，即时类操作加 q 乘子）

`dmg = clamp( (raw − mit) × (100+kè)/100 × q_factor, Floor, PowerCap )`
- `raw/mit/kè` 同 R2（攻击维/招式 Tier/招架闪避/相克）。
- `q_factor`：若防御方触发即时反应，按 q 衰减入伤（格挡 q 高→入伤大减；闪避 q 高→近乎免伤）。非即时操作 q_factor=1。

---

## Edge Cases

- **若 speed 完全相等**（同路镜像）：行动条同时满 → 按 CharacterId 序定先后（确定 tiebreak）。
- **若一击致死但防御方有"硬抗/不灭金身"**：先判被动触发（HP 将≤0 时），成功则 HP 保留至阈值、消耗续命资源；失败正常死亡。
- **若多个即时反应同时可触发**（既能闪又能格）：按功法门控优先级（玩家选/NPC 取 q 期望最高者），只触发一个。
- **窗口外应对（Δ>window）**：q=0，操作失败（闪空/格崩），且可能有后摇惩罚（落空硬直）。
- **NPC vs NPC 无玩家**：所有即时窗口走 q_npc 概率采样，纯 Core 结算，headless 可跑、逐字节复现。
- **夺舍续命失败**（遇雷/纯阳/佛光在场）：确定性判定（非掷骰），失败=永久退场。
- **逆演回滚**：只撤上一次交锋结算（伤害/夺运/胜负），**Karma/天谴债不回滚**（防无限重开）。
- **off（cultivation-off）**：无 Cultivation，不入本系统，走 legacy `SparAction`（红线 B.3 逐字节守）。
- **速度无限叠（haste 类）**：speed 设 cap，防一方无限连动。

---

## Dependencies

| 系统 | 方向 | 接口 |
|---|---|---|
| PowerEngine（修炼） | 上游 | 提供 pe（=战斗 HP + 伤害量级锚） |
| 模块化效果系统（EffectOp/ModuleResolver/Special） | 上游 | 操作的效果 = EffectOp 模块；即时类加 q 乘子 |
| 21 路 CultivationPathDef | 上游 | CombatSkills（操作）+ ArtCategories（功法门控能力来源）+ Tier（品级→窗口宽度） |
| SituationalResolver（相克） | 上游 | kè 软情境 adj（±P0/4） |
| 平衡标定 INV-CROSS | 横向 | 校准 speed/window/Floor 等旋钮使 C1 胜率∈[40,60]% |
| Pcg32 随机流 | 上游 | NPC q 采样的确定性扰动（新流需升 World 字段+进 Clone，红线 B.2） |
| Unity 宿主层（后期） | 下游 | 即时窗口的实时呈现 + 玩家按键采集 → 喂 q |
| Chronicle/叙事 | 下游 | DuelResolved（winner/margin）+ 可选 narrator 逐回合解析 |

---

## Tuning Knobs

| 旋钮 | 默认 | 安全域 | 调高/调低影响 |
|---|---|---|---|
| ACTION_THRESHOLD | 1000 | 500–2000 | 高→行动慢节奏长；低→快节奏 |
| Ks（悟性→speed） | 2 | 1–4 | 高→悟性主导先手；低→各路趋同 |
| 回合上限 N | 不固定（HP 决） | — | 防战斗无限拖（cap 兜底） |
| window₀（各操作） | 见公式表 | ±50% | 宽→即时操作好做（偏休闲）；窄→硬核 |
| 品级系数斜率 | tier×12~20 | — | 大→高功法反应碾压；小→品级影响弱 |
| Q_MAX | 100 | — | 即时反应满效果上限 |
| Floor/PowerCap | 承 INV-CROSS | — | 保底伤/伤害上限 |
| NPC base_skill | 难度档 | 0–100 | 高→NPC 反应强（高难）；低→NPC 笨 |

---

## Visual/Audio Requirements

> 当前 v1 为 CLI 文本快照；以下为 Unity 阶段（即时窗口的呈现是即时层的核心体验）。

- **即时窗口视觉**：敌方杀招起手时，目标身上/招式轨迹给"窗口提示"（如闪光带、读条收束圈），窗口中心有明确视觉峰值（33 号式 parry 提示）。
- **q 反馈**：完美应对（q 高）= 强特效（格挡火花迸溅、闪避残影、点穴定格）；勉强（q 低）= 弱反馈；失败（q=0）= 落空硬直动画。
- **速度序列**：行动条 UI（CTB 队列条），显示谁下一个动。
- **门派质感**：剑修剑光、雷修紫电、毒蛊绿雾、佛修金光——承 B.8 像素轨 + AI 静态特效件（已建出料管线）。
- **音**：即时窗口成功有清脆命中音；蓄力有读条音（被打断戛然而止）。

📌 **Asset Spec**：本节定后，Unity 阶段可 `/asset-spec system:combat` 出特效/UI 资产规格。

---

## UI Requirements

- **即时窗口 UI（Unity 核心）**：窗口提示条 + 按键提示 + q 评级反馈（完美/良/失败）。
- **行动序列条**：CTB 队列可视化。
- **操作选择**：回合 choice 类操作的选招菜单（招式 + 资源消耗 + 门控可用性）。
- **战斗 HUD**：双方 HP（=pe）、资源条（剑意/血气/煞值…）、状态（控制/DOT/BUFF 图标）。

📌 **UX Flag**：Unity 阶段先 `/ux-design` 出即时窗口 + 战斗 HUD 的 UX 规格，再写 stories。

---

## Acceptance Criteria

- **GIVEN** 两 NPC 同 UT 不同路，**WHEN** 跑 1000 场 Core 战斗，**THEN** 胜率∈[40,60]%（C1 契约，纯 Core 确定可复现）。
- **GIVEN** A speed 远高于 B，**WHEN** 战斗，**THEN** A 行动次数显著多于 B（CTB 生效）。
- **GIVEN** 防御方修了轻功且持闪避操作，**WHEN** 被攻击且 q 采样高，**THEN** 入伤大幅衰减；**未修轻功**则无闪避能力（功法门控）。
- **GIVEN** NPC×NPC 无玩家，**WHEN** 即时窗口操作触发，**THEN** 走 q_npc 概率采样、headless 跑通、同种子逐字节复现（B.2）。
- **GIVEN** 攻击方蓄力中，**WHEN** 防御方在打断窗口内 q 高，**THEN** 蓄招被打断、不结算。
- **GIVEN** 防御方濒死且会夺舍，**WHEN** 夺舍窗口 q 成功且无雷/纯阳/佛光，**THEN** 续命；**否则**永久退场。
- **GIVEN** cultivation-off，**WHEN** 切磋，**THEN** 走 legacy SparAction、与 v1.0 逐字节一致（B.3）。
- **GIVEN** 逆演触发，**WHEN** 回滚，**THEN** 撤上一次交锋但 Karma 不回滚。
- **GIVEN** 品级越高的同类闪避功法，**WHEN** 同等偏差应对，**THEN** 高品级 q 更高（窗口更宽，容错验证）。

---

## Open Questions

- **窗口宽度具体数值**：window₀ 表是初值，需 INV-CROSS 标定阶段对拍校准（属 balance-cross epic）。
- **NPC q 采样的难度曲线**：不同江湖角色（杂兵 vs 高手）的 base_skill 怎么分布？依赖 AI/难度系统设计。
- **即时窗口在多人混战**（>2 角色）如何处理：先按 1v1 落地，群战窗口规则待评估。
- **攻击方是否也有窗口**（用户②留口：蓄力重击在窗口内释放伤害更高/连击踩点）：本版聚焦防御侧即时窗口，进攻侧即时留作扩展。
- **逆演/夺舍/落宝的 Special handler**：batch3 已 wiring 8 个，但"真濒死判定/栈回滚"依赖 batch4 回合态（已显式 defer）。
