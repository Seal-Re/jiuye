# 抓取/调研 scratch 产物（大、可重生成，不入库）
.playwright-mcp/
_research/raw/
_research/raw2/
_paper_text.txt
*.output

# .NET 构建产物
bin/
obj/
*.user

# IDE
.vs/
.idea/

# === CCGS 管理骨架（手工并入·只取安全行；绝不加 *.sln/*.csproj/[Oo]bj/ 等会吞 .NET 工程文件的行）===
# CCGS 本地/瞬态
.claude/settings.local.json
CLAUDE.local.md
production/session-state/
production/session-logs/
docs/consistency-failures.md
# 凭据/密钥（绝不入库）——SunshineFlow 等 skill 的 .env
.env
**/config/.env
.claude/skills/**/config/
# Python 工具产物
__pycache__/
*.pyc
# EPIC-TIDY 顺手补（像素产物 + 抓取 raw3）
# 注: 顶层 pixel/ 已迁 → tools/pixel-pipeline/(脚本) + assets/art/pixel/(资产)，且本地残留已清
assets/art/pixel/out/
tools/pixel-pipeline/out/
_research/raw3/
# AI 文生图 raw 产物（精修入库的才进 tools/pixel-pipeline/parts/）
tools/pixel-pipeline/_aigen/
# map-renderer 验证产物（可重生成，不入库）
output/
